# Feature 040 — CPU-script engine (uniform-compute scripts)

> Status: IMPLEMENTED (2026-06-13) — landed in 5 commits on `dev` (`9924d58`..`c5c2d0a`) + a
> post-impl review-fix commit (`034db20`); NOT yet shipped (last release is v0.15.0). v1 = the engine
> ONLY, headless, no in-app UI. Scripts are hand-placed on disk + hot-reloaded; verified by pure-CPU
> unit tests + a GL-integration test + a headless dogfood determinism check (all green on the Pi's
> V3D). Post-impl review CONVERGED (see Review history). The five plan-lock open questions are
> resolved inline in the Design decisions below.

## Goal

Make a uniform a first-class object you attach a behavior SCRIPT to: a small CPU-side script computes the uniform's value every frame, turning ShaderBox from a static-uniform playground into the first step of a mini game-engine. v1 ships the engine core only — a new `shaderbox/scripting/` package that ProjectSession ticks once per frame to write computed values into each `Node.uniform_values` BEFORE `Node.render()` reads them. The script ON DISK is body-only Python: the user writes just the function body and hands the result back through a passed-in `out` object (`out.set(...)`), with `ctx` (the read-only world) also in scope. Binding is by filename (`nodes/<id>/scripts/u_<name>.py` → uniform `u_<name>`). The language is a swappable backend behind a `Behavior` protocol so a future C/native backend drops in with no engine-loop change. Errors are data, never a crash: a broken script freezes the uniform at its last-good value and records a `ScriptError`. A prototype at `/tmp/sb_engine_proto/` validated the shape (body-only `out.set()`, curated namespace, error-as-data with the SyntaxError pointing at the user's source line, scoped live↔export determinism, ~3-4us/tick).

## Out of scope (each deferral carries a trigger)

- **In-app UI (editor, "script-driven" slider indicator, attach/detach affordance).** v1 scripts are hand-placed on disk + hot-reloaded. Trigger: feature **041** (system-input uniforms `u_mouse` + the in-app script editor) — that feature adds the visual indicator that a uniform is script-driven and the affordance to create/edit a script in-app.
- **System-input uniforms in `EngineContext` (`mouse`, `state`, `uniforms` view).** The context fields exist (reserved) but v1 never populates `mouse` and never runs phase A. Trigger: **041** wires real `u_mouse`; **042** populates and reads `ctx.state`.
- **Phase A (state-evolution scripts).** v1 runs phase B (uniform compute) only; phase A is a stub with no state objects. Trigger: feature **042** (state scripts → enables pong). The phase-A→B ordering invariant (A mutates `ctx.state`; B reads it; both before `Node.render`) is documented in Design decision 1 BEFORE 042 lands.
- **Copilot write-behavior tool.** v1 scripts are hand-placed; no copilot tool writes them. Trigger: feature **043** (copilot write-behavior tool). NOTE: the `todo.md` "[DEFERRAL] copilot turn-rollback: a NEW mutating tool must register with the checkpoint" is NOT triggered by 040 (no new mutating copilot tool, scripts are hand-placed) — it fires at **043**, where the write-behavior tool must `_capture_node`-equivalent the script file into the active checkpoint.
- **`numpy` in the script namespace.** v1 exposes `math` helpers only (sin/cos/sqrt/clamp/lerp/pi/tau/…), NOT `np` — per-tick numpy allocation is a perf footgun and a vec result is fine as a plain tuple. Trigger: a real vector-math script proves a tuple-only namespace too painful (revisit then, scoped to exposing a curated subset, not the whole module).
- **Type-change / orphan cleanup UI.** Because binding is by filename and `UIUniform` is hash-keyed on `(name, type)` (`util.py::get_uniform_hash`), changing `vec2 u_x` → `vec3 u_x` keeps the file bound but the script returns the wrong shape → a coercion error every tick; an orphaned script (uniform deleted from the shader) just sits unused. v1 resolves script→uniform on load and WARNS (logged) on orphan / shape-mismatch; the value still freezes at last-good and surfaces a `ScriptError`. Trigger: feature 041's editor (full detach/cleanup affordance lands with the UI).
- **`set_uniform` "script-driven" visual indicator.** v1 makes the copilot `set_uniform` REJECT a script-driven uniform (Design decision 5), but the UI slider indicator is deferred. Trigger: **041** (no UI in v1).
- **`u_*.py` discovery for non-scalar/non-vector uniform kinds.** v1 scripts only target scalar + small-vector uniforms (the kinds the namespace can produce — see Design decision 7). Sampler/UBO/array uniforms are out. Trigger: a concrete need to script an array or texture uniform (revisit the namespace + coercion reuse then).

## Design decisions (numbered; lock-in only)

> Convention form: "we decided X; revisit if Y." The five plan-lock open questions were resolved by the maintainer — see the *Plan-lock resolution log* at the end.

1. **`EngineContext` shape + the two-phase contract.** A single frozen-ish dataclass `EngineContext(t: float, dt: float, frame: int, mouse: tuple[float, float] = (0.0, 0.0), state: dict[str, Any] = {}, uniforms: dict[str, Any] = {})` is the one object every script receives. `t/dt/frame` are the clock (live: from `glfw.get_time`; export: deterministic `t = i/fps`, `dt = 1/fps` — see decision 4). `mouse/state/uniforms` are RESERVED for 041/042 — present in the shape so the protocol is stable, but v1 leaves `mouse=(0,0)` and `state={}`. The tick runs **two phases**: A (state evolution — mutates `ctx.state`; v1 STUB, no state objects) then B (uniform compute — writes `node.uniform_values[name]` for each scripted uniform). The invariant: **A runs to completion before any B reads `ctx.state`, and the whole tick runs before `Node.render()` reads `uniform_values`.** v1 runs only B. **Determinism is SCOPED, not universal:** a script that is a pure function of `ctx.t` (e.g. `sin(ctx.t)`) yields the same value at the same `t` in live and export. A script that reads `ctx.dt` or the prev-frame value via `ctx.uniforms` (an integrator/smoother — the proto's `u_follow`) is PATH-DEPENDENT and WILL diverge between live (variable dt) and export (fixed `1/fps`). This is acknowledged, not a bug: the determinism guarantee holds only for `ctx.t`-pure scripts; integrators are frame-rate-dependent by nature. The verification (unit + dogfood) asserts determinism ONLY on a `ctx.t`-pure script, and a separate test documents the integrator-divergence as expected. Revisit the context fields if 042's pong needs state shapes a `dict[str, Any]` can't carry.

2. **Body-only on disk; value handed back via an OUTPUT OBJECT, NOT a last-expression rewrite (resolves blocker 1 — locked Q2).** The script file is the function BODY only — no signature/imports persisted, ONE representation on disk, no dual-representation sync (the 039 lesson). The engine executes the body with two names in scope: `ctx` (the read-only world — `EngineContext`) and `out` (the value sink for THIS uniform). The user assigns the result by calling `out.set(...)`:
   ```python
   # scripts/u_wave.py
   out.set(0.5 + 0.3 * sin(ctx.t))
   # scripts/u_offset.py  (vec2)
   r = 0.4
   out.set(r * cos(ctx.t), r * sin(ctx.t))
   # scripts/u_color.py  (branching is natural — no wrapper edge cases)
   if ctx.t > 1.0:
       out.set(1.0, 0.0, 0.0)
   else:
       out.set(0.0, 0.0, 1.0)
   ```
   **The proto at `/tmp/sb_engine_proto/` STILL uses the OLD "rewrite the last line to `__out = (…)`" approach (engine.py:122) — it is OUTDATED relative to this spec and must be updated to the `out.set()` shape before it's cited as validation (a Files-touched/verification item below).** That rewrite is REJECTED here: it was a hidden source transform that broke on a statement/comment/multiline-expr last line and pointed a `SyntaxError` into the WRAPPER, not the user's source (the 039 ghost). The output-object design removes that class BY CONSTRUCTION: the engine NEVER rewrites the body — it `exec()`s it verbatim with `ctx` + `out` injected into the namespace dict (injection is a namespace entry, NOT a source edit), so a `SyntaxError`/runtime error's `lineno` already points at the user's source — no AST surgery, no line-remap needed.
   **How `out` knows the uniform shape (the metadata gap):** `out` is a `UniformOut` object the ENGINE constructs per scripted-uniform per-tick, injected with the live `moderngl.Uniform` (so it holds `(dim, array_length, gl_type)`). `out.set(*args)` accepts a scalar (`out.set(0.5)`), per-component vec args (`out.set(x, y, z)`) or a single sequence (`out.set((x, y, z))`); it normalizes `*args` to a single value then validates the shape against its uniform via the reused coercion (decision 7 — `coerce_uniform_value(value, self._uniform)`), and on mismatch records a `ScriptError` (kind=`runtime`) with the `shape_hint` message, freezing last-good. The body never sees the `moderngl.Uniform` — only `out.set`. **Not calling `out.set()` is valid** — the uniform simply holds its previous value that frame (a no-op tick; useful for "update only when there's something to say"); a dedicated test covers this. **Multiple `out.set()` calls** in one tick = last-wins (documented, not an error). Revisit only if a uniform genuinely needs MULTIPLE outputs from one script (then `out` grows named sinks — not a reshape of the single-uniform contract).

3. **`Behavior` backend abstraction (language-swappable).** A `Behavior` Protocol with `compute(ctx: EngineContext, out: UniformOut) -> None` is the seam that makes the script LANGUAGE a detail (the script writes its result into `out`, decision 2). v1 ships `PythonBehavior` (compile the raw body once into a code object — no AST rewrite, decision 2 — and `exec()` it each tick against a curated namespace with `ctx`/`out` injected). A future C/native backend (`CBehavior` loading a `.so`, calling its entry that fills `out`) implements the SAME protocol — the engine loop iterates `Behavior` objects and never knows the language. A compiled `Behavior` caches its own compile-phase `ScriptError` (a `SyntaxError` in the body makes a `Behavior` that always freezes). Revisit if a backend needs a richer contract (e.g. a phase-A `update_state(ctx)` — that's 042, a SECOND protocol method, not a reshape of `compute`).

4. **Export must route through the engine tick (resolves blocker 2).** Today `core.py::_render_video` and `_render_image` call `Node.render()` DIRECTLY, bypassing the engine — so an exported video would have FROZEN scripted uniforms while live preview animates (silent live↔export divergence). Export MUST tick `(t, dt, frame)` before each `node.render()`. We do NOT pass ProjectSession into Node (breaks the 025 boundary). The locked seam:
   The boundary constraint: `Node` (App-free, 025) must not import or hold `ProjectSession`; the engine reaches the export frame loop via a callback the session injects onto `Node` at load. (Two shapes were weighed — a ProjectSession wrapper that owns the export loop vs. a callback on `Node` — and the callback won because `_render_video`/`_render_image` already live on `Node`, so the loop stays there and only the per-frame tick is injected.)
   - **LOCKED (Q1)** — a tick→render seam the engine owns, via the injected callback. `_render_video`/`_render_image` (core.py:401/489) call `Node.render()` directly TODAY; they must instead tick-then-render per frame. **Caller graph (verified — this is the real blast):** `render_media`→`_render_media_into`→`_render_video`/`_render_image` is reached from THREE places: the Render tab (`tabs/render.py:60` → `node.render_media`), the share scratch path (`tabs/share_state.py:99` `render_to` → `render_media`), and the COPILOT render tools (`copilot/backend.py:909/935/1000` → `share_state.render_to`). So the seam must sit where the per-frame `Node.render(u_time=...)` call is made — INSIDE the export loop (`_render_video` 489-499, `_render_image` 401-423) — and the engine tick runs there.
   - **The hook fires ONLY in the export loop — NEVER in `Node.render()` (resolves the round-2 double-tick risk).** `Node` gains an optional `on_pre_render: Callable[[float, float, int], None] | None` the session sets at load; it is invoked by the EXPORT entry points (`_render_video`'s per-frame loop before each `self.render(i/fps, ...)`; `_render_image` before its single `self.render(u_time=..., ...)` — passing that frame's `t/dt/frame`), and is **NOT called from `Node.render()` itself.** This is critical: the LIVE path already ticks via `session.tick()` in `ui.py::update_and_draw` (before the render block); if `render()` ALSO fired the hook, the live frame would tick TWICE (once in ui.py, once in render) — a uniform-value corruption. So: **live ticks once (ui.py `session.tick`); export ticks once per frame (the export-loop hook); `Node.render()` never ticks.** The copilot render reaches `_render_*` via `share_state.render_to`→`render_media` and is bridge-marshalled to the main thread (`backend.py run_on_main`), so its tick is a main-thread CPU write — no threading hazard.
   - **Tick scope = every node that will render THIS frame, matching the live render-gate (resolves the round-2 scope question).** `ui.py::update_and_draw` renders the current node always, and all nodes when `is_render_all_nodes` (ui.py:135-141). `session.tick()` must tick exactly that set (the same gate), so a scripted uniform on a non-current node animates identically live and in export. Export ticks the single node it renders. (v1 keeps this simple: tick the same nodes the live frame renders; don't tick popup-suppressed frames, matching the existing render skip.)
   - Rated **HIGH blast-radius** (a multi-caller hot-path touch), not MED — reflected in Review sizing + the blast table.

5. **`set_uniform` rejects a script-driven uniform (resolves blocker 3).** A script writing `u_x` every tick would silently no-op a UI slider and the copilot `set_uniform` (`backend.py::set_uniform`). The copilot `set_uniform` MUST reject a script-driven uniform with a clear, model-facing error — mirroring the existing engine-driven rejection at `backend.py` (`if name in ENGINE_DRIVEN_UNIFORMS: … "is engine-owned … cannot be set"`) and the sampler/block rejection just below it. The check needs ProjectSession to expose "which uniforms on node N are script-driven" (the engine owns that set per node); `set_uniform` queries it via an injected getter (same pattern as the existing `get_ui_nodes` etc. getters on `CopilotBackend`). The UI slider visual indicator is DEFERRED to 041 (Out of scope). Revisit the message/seam if 043's write-behavior tool needs the inverse (attach a script TO a uniform that currently has a hand-set value).

6. **Binding by FILENAME; no `node.json` entry.** `nodes/<id>/scripts/u_<name>.py` binds to uniform `u_<name>` — the filename IS the binding, stateless. `UINode.save` writes NO scripts entry into `node.json` and `UIUniform` gains NO persisted binding field (confirmed: `ui_models.py::UINode.save` serializes uniform VALUES + `ui_state`; a script binding is not uniform state, it's a sibling file). The engine resolves script→uniform at load by globbing `scripts/u_*.py` against the node's active uniforms. Revisit if a binding ever needs metadata the filename can't carry (then it's a manifest, and that's a deliberate reversal).

7. **Scriptable uniform kinds + reuse of copilot coercion (home LOCKED — Q4).** v1 scripts target the uniform kinds a Python value can produce: scalar (`float`/`uint`, `dim==1, array_length==1`), small vector (`vecN`, `dim in 2..4, array_length==1`), and the existing array kinds the coercion already handles (`float[N]`, the `uint[N]` text array). Samplers and UBOs are NOT scriptable. `out.set(...)` (decision 2) validates + shapes its arg by REUSING `_coerce_uniform_value` (do NOT reinvent coercion): a value that doesn't fit the uniform's `(dim, array_length, gl_type)` is a runtime `ScriptError` carrying `_set_uniform_shape_hint`'s message, frozen at last-good. **LOCKED: `_coerce_uniform_value` + `_is_number` + `_coerce_array` + `_set_uniform_shape_hint` move to a NEW leaf `shaderbox/uniform_coerce.py`** (public names, drop the leading underscore) that both `copilot/backend.py` and `scripting/` import — a dedicated leaf reads cleaner than growing `util.py`, and breaks the cycle (the engine must not import `copilot/`). Revisit the scriptable-kind set when a real sampler script need appears.

8. **`ScriptError` as data + last-good freeze (mirrors the shader-error precedent).** A frozen `ScriptError(uniform_name: str, kind: Literal["compile","runtime"], message: str, line: int = -1)` mirrors `shader_errors.ShaderError` (error-as-data; `line` is the user-source line via the decision-2 remap, `-1` when unmappable). A compile failure (bad AST / `SyntaxError`) makes the `Behavior` permanently error until the file changes; a runtime failure (`exec` raises, coercion returns None) is caught per-tick. EITHER way: the uniform FREEZES at its last-good value (its current `node.uniform_values[name]`, or the node-intrinsic default if never computed — via `Node.seed_uniform_values`), the frame CONTINUES, and the error is recorded on the engine state (`dict[uniform_key, ScriptError]`) for 041's UI to surface. Never raises into the frame loop. Revisit if a third error kind earns a branch (e.g. a "shape-mismatch" distinct from generic runtime — only if 041's UI wants to render it differently).

9. **Hot-reload cache keyed by `(path, mtime)`; watcher lives IN the engine (Q5 LOCKED).** The compiled `Behavior` is cached keyed by `(path, mtime)`; on a poll, recompile ONLY scripts whose mtime changed — never recompile all scripts every frame. A new/removed `u_*.py` file is also detected (glob + stat, like `maybe_rebuild_lib_index`). **LOCKED: the poll method lives on the script engine (`scripting/engine.py`), invoked from `ui.py::update_and_draw`** (after the lib/node watch, before render) for live, and run once at load for export — keeping all scripting machinery self-contained rather than scattering a script-specific watcher into `watch.py`. Revisit if per-frame glob+stat over the scripts dir shows up in a profile (then cache the dir listing + invalidate on a coarser signal).

10. **ProjectSession placement (App-free, 025 boundary).** The engine is REPO code owned by the headless `ProjectSession` (`project_session.py`) — it imports no imgui/glfw and constructs no window/context (the 025 invariant). ProjectSession gains: the per-node script registry (compiled `Behavior` objects + their `(path, mtime)` cache), the per-node `ScriptError` map, the (v1-empty) `state` dict, and the `tick(t, dt, frame)` method that runs phase A (stub) then phase B (writes `node.uniform_values`). Scripts are PROJECT DATA (live under the project's `nodes/<id>/scripts/`); the engine is repo code. App's frame loop calls `session.tick(...)` before render (live); export routes through the session per decision 4. Revisit if the engine grows state that genuinely needs to live closer to GL than the headless core (it should not — the engine is pure CPU).

11. **New `shaderbox/scripting/` package shape.** A new leaf-ish package (imports `shader_errors` for the error precedent + the shared coercion home; imports NO imgui/glfw/App, NO `core.Node` type beyond what it needs to write `uniform_values` — it works against the `node.uniform_values` dict + `get_active_uniforms()`, not the GL program). Proposed modules:
    - `scripting/context.py` — `EngineContext` dataclass (leaf).
    - `scripting/errors.py` — `ScriptError` (leaf, mirrors `shader_errors`). (May merge into `engine.py` if it stays tiny.)
    - `scripting/behavior.py` — the `Behavior` Protocol + `PythonBehavior` (the namespace, the `compile()`/`exec()` seam, the `UniformOut` sink + its shape validation via the reused coercion).
    - `scripting/engine.py` — the script registry per node, the `(path, mtime)` cache, the load/resolve-against-uniforms + orphan/mismatch warning, and the `tick` body (phases A stub / B compute) that `ProjectSession.tick` drives. `ProjectSession.tick` is the public entry; the package internals are not reached by UI/App directly.
    - The curated namespace (`math`-only, no numpy — decision/Out-of-scope) lives in `behavior.py` as a module-level builder.

12. **The exec() seam needs ONE sanctioned `# type: ignore` (or `# noqa`).** Raw `compile()` + `exec()` with a curated namespace, NO sandboxing (personal IDE, projects are not shared, max perf — the locked posture). `exec` against a dict trips ruff's `S102` and possibly a pyright `Any`-flow note; this is the ONE sanctioned suppression for the engine seam. It MUST be added to the `conventions.md ## Known quirks` sanctioned-suppression allowlist in the same wave (with the rationale: "the scripting engine's `exec()` of a user body — no-sandbox is a locked decision, not a stub"). No OTHER suppressions land. Revisit only if the no-sandbox posture is ever reversed (it won't be for a personal IDE).

## Files touched (mapped to the dev_flow module map)

NEW package — **`shaderbox/scripting/`** (a new entry in the module map; the headless CPU-script engine, App-free, imports no imgui/glfw):
- `scripting/__init__.py` — public surface (`EngineContext`, `ScriptError`, `Behavior`, the engine entry the session calls).
- `scripting/context.py` — `EngineContext` dataclass (decision 1).
- `scripting/errors.py` — `ScriptError` (decision 8; may fold into `engine.py`).
- `scripting/behavior.py` — `Behavior` Protocol + `PythonBehavior` (verbatim `compile()`/`exec()`, NO body rewrite) + the `UniformOut` value-sink (holds the live `moderngl.Uniform`, `set(*args)` → reused coercion) + the curated `math`-only namespace (decisions 2, 3, 7, 12).
- `scripting/engine.py` — per-node script registry, `(path, mtime)` cache, load/resolve + orphan/mismatch warning, the two-phase `tick` body, the `pre_render` hook the export path uses (decisions 4, 8, 9, 10, 11).

Existing files:
- **`shaderbox/project_session.py`** — add the engine state (per-node `Behavior` registry, `ScriptError` map, `state` dict) + a `tick(t, dt, frame)` method that drives phase A (stub) / phase B (writes `node.uniform_values` for scripted uniforms); load/resolve scripts in `load()` (after `load_nodes_from_dir`); set each node's `pre_render` hook to the engine tick at load (decision 4). Expose `get_script_driven_uniforms(node_id) -> set[str]` (the set of uniform names with a `u_<name>.py` file) for the copilot rejection (decision 5). Decision 10/11 home.
- **`shaderbox/core.py`** — **HIGH-blast hot-path touch.** `Node` gains an optional `on_pre_render: Callable[[float, float, int], None] | None` the session sets at load. It is invoked ONLY from the EXPORT loop — inside `_render_video` (before each `self.render(i/fps, ...)` at ~492) and `_render_image` (before its single `self.render(u_time=t, ...)` at ~405), passing that frame's `t/dt/frame`. **It is NOT invoked from `Node.render()` itself** — the live path ticks via `session.tick()` in `ui.py`, so firing the hook inside `render()` would double-tick the live frame (round-2 finding). `_render_image` must also be given a caller-controlled `t` (it defaults `u_time=0.0` today, core.py:402) so the dogfood/export can advance it. This keeps `Node` from importing ProjectSession (the engine fills the hook). Verified callers reaching the export path: `tabs/render.py:60`, `tabs/share_state.py:99` (`render_to`), `copilot/backend.py:909/935/1000` — ALL get the tick for free via the export-loop hook. `ENGINE_DRIVEN_UNIFORMS` is UNCHANGED (script-driven is a per-node engine fact, NOT a global engine-driven name).
- **`shaderbox/ui.py`** — `update_and_draw` calls `app.session.tick(t, dt, frame)` AFTER the node/lib mtime watch (so a just-reloaded script is live) and BEFORE the live render block (109/141). It ticks **exactly the nodes that frame will render** — the same gate as the render block (current node always; all nodes when `is_render_all_nodes`; nothing when a popup suppresses the render) — so live and export animate the same set (decision 4 scope rule). `dt` derives from the existing `glfw.get_time()` delta in `run()` (thread the frame `t/dt/frame` to the tick call site). **This wiring is the PREREQUISITE for the smoke assertion to mean anything** (see Manual verification §4).
- **`shaderbox/watch.py`** — UNCHANGED for scripts: the script `(path, mtime)` poll lives on the engine (`scripting/engine.py`, Q5 LOCKED), invoked from `ui.py::update_and_draw` beside `reload_node_if_changed`. Listed only so the reviewer confirms the script watcher does NOT get bolted into `watch.py`.
- **`shaderbox/paths.py`** — `ProjectPaths` documents the per-node `scripts/` dir convention (`nodes/<id>/scripts/`). LAZY (globbed-if-exists at load; created on first write by 041/043) — NOT a new eagerly-mkdir'd dir (`for_root`'s 5-dir mkdir set is unchanged). A `scripts_dir_for(node_id)` helper lives here (path resolution is `paths.py`'s job).
- **`shaderbox/uniform_coerce.py`** (NEW leaf, Q4 LOCKED) — the new HOME for `coerce_uniform_value` / `is_number` / `coerce_array` / `uniform_shape_hint` (hoisted from `copilot/backend.py`, underscore dropped → public). Imported by `copilot/backend.py` AND `scripting/behavior.py`. **Blast (review-corrected to LOW-MED, not LOW):** the move + private→public rename has THREE import sites that must update in the same commit — `copilot/backend.py` (the internal calls at 266/268/767/770), `tests/test_cross_project_tools.py:17`, `tests/test_uniform_arrays.py:8` (30+ assertions on `_coerce_uniform_value`). A mechanical but multi-site rename; do it as its own commit-step.
- **`shaderbox/copilot/backend.py`** — `set_uniform` gains the script-driven rejection branch (decision 5), mirroring the `ENGINE_DRIVEN_UNIFORMS` / sampler reject shape (744-765); the coercion calls now import from `uniform_coerce`. The constructor gains the `get_script_driven_uniforms` injected getter, wired in `ProjectSession._build_copilot_capabilities` (same pattern as the existing `get_ui_nodes` getters).
- **`shaderbox/ui_models.py`** — CONFIRMED NO CHANGE for binding persistence: `UINode.save` writes no scripts entry, `UIUniform` gains no binding field (decision 6). The engine reads `(dim, array_length, gl_type)` off the live `moderngl.Uniform`, leaving `ui_models.py` untouched.
- **`/tmp/sb_engine_proto/engine.py`** (the prototype) — UPDATE from the rejected last-expression rewrite to the `out.set()` shape so it matches this spec and can be cited as the validated reference (review-flagged: the proto is currently OUTDATED vs the spec). Not shipped (it's in `/tmp`), but it's the impl reference + the source of the unit-test cases.
- **`scripts/dogfood/harness.py`** — add a `render(t=...)` (or tick-then-render) entry so a scenario can render the same node at distinct `t` values (today `render` only hits t=0 via `_render_image`'s default — the dogfood determinism check needs this; decision 4 / Manual verification §5).
- **`scripts/smoke.py`** — seed a scripted node, run 200 `update_and_draw` frames, assert **no crash** (the integration smoke role). The engine-CORRECTNESS claim (values, freeze, determinism) lives in the pure-CPU unit test, NOT smoke — so smoke does NOT assert `errors`-empty as its correctness proof (that would pass trivially before the ui.py tick wiring, and is redundant with the unit test after). Manual verification §4 option (b) is the chosen split: unit test owns correctness, smoke owns "doesn't crash with a scripted node in the loop."
- **`ai_docs/conventions.md`** — add the `exec()` sanctioned-suppression entry to `## Known quirks` (decision 12); add a `## Design decisions` bullet for the scripting-engine boundary ("the CPU-script engine is headless ProjectSession code; scripts are project data; a new script LANGUAGE is a `Behavior` backend, not an engine-loop change") with its revisit trigger.
- **`ai_docs/roadmap.md`** — add/flip the 040 row + rewrite the Active-context banner ("what's next") at done.
- **`ai_docs/todo.md`** — note the 042 phase-A→B documentation trigger + the 043 copilot-rollback/write-behavior trigger if not already covered; the type-change/orphan-cleanup deferral with its 041 trigger.

NEW tests (mirror the `tests/*.py` + `scripts/` patterns):
- `tests/test_script_engine.py` — PURE-CPU unit tests (no GL): a scalar `out.set(...)` body; a temps-then-`out.set` body; a branching body (`out.set` in each branch); a **body that never calls `out.set()`** (uniform holds last-good, no error); **multiple `out.set()` calls** (last-wins); a `SyntaxError` in the body → compile `ScriptError` pointing at the USER source line (verbatim exec, no remap); a runtime error (`exec` raises) freezing last-good + recording a `ScriptError` without raising; a shape-mismatch in `out.set` → `ScriptError` via reused coercion; **determinism asserted ONLY on a `ctx.t`-pure script** (same `t` → same value) + a separate test DOCUMENTING that a `ctx.dt`/`ctx.uniforms` integrator diverges by design (decision 1); the `(path, mtime)` cache (no recompile when mtime unchanged, recompile on change); the perf probe (sanity, not a gate).
- `tests/test_script_engine_gl.py` — a GL-integration test on a standalone EGL/GL context (the `tests/test_uniform_seed_save.py` `create_standalone_context()` fixture pattern; `pytest.skip` if none — NOTE: skips on the display-less dev box unless `MESA_GL_VERSION_OVERRIDE=4.6` is set per `todo.md`, so it's a CI/explicit-EGL gate, not a default-dev-box gate): a node + a scripted scalar + a scripted vec2, ticked then rendered, asserts `node.uniform_values` carries the computed value, that the rendered pixel CHANGES between two `t` values (value reaches the GPU), and that a shape-mismatch freezes + records a `ScriptError`.
- `tests/test_copilot_*.py` (extend existing) — assert `set_uniform` REJECTS a script-driven uniform with a clear error (decision 5; the rejection lives in the copilot layer and is otherwise untested by the engine tests).

## Manual verification

> **The dev box is a display-less Raspberry Pi** (`todo.md` "[DEFERRAL] headless GL on a display-less Linux box"). `make run` cannot be eyeballed by the agent here; the agent verifies on the **headless backbone** and hands the live visual confirmation to the maintainer, plus a required dogfood scenario.

**Headless backbone (the agent runs these):**
1. **Pure-CPU unit tests** (`uv run pytest tests/test_script_engine.py`) — the drive.py invariants above. No GL, runs on the Pi.
2. **GL-integration test** (`uv run pytest tests/test_script_engine_gl.py`) — standalone EGL context (`backend="egl"` mirrors `scripts/dogfood/harness.py`; `create_standalone_context()` mirrors `test_uniform_seed_save.py`); skips cleanly if no GL. Asserts the tick writes into `node.uniform_values` and the coercion/freeze path on a shape-mismatch.
3. **`make check`** — pyright at 0 errors (confirm the ONE sanctioned `exec()` suppression is the only new marker) + ruff.
4. **`make smoke`** — the extended smoke (a seeded scripted uniform ticks clean for 200 frames, `errors` empty). **ORDERING (review-flagged): the smoke assertion is meaningless until `ui.py::update_and_draw` actually calls `session.tick()`** — today `update_and_draw` never ticks, so an `errors`-empty assert would pass trivially (tick never ran). So either (a) the smoke assertion lands ONLY after the ui.py wiring in the same impl, OR (b) move the "ticks clean" assertion into the pure-CPU unit test (which calls the engine directly) and keep smoke's role to "App-with-a-scripted-node builds + runs 200 frames without crashing." Prefer (b) for the engine-correctness claim; (a) only as the integration smoke. Skips loudly on the display-less box (`_has_gpu_window()` already gates).
5. **Dogfood scenario (REQUIRED, since `make run` is uneyeballable here).** Drive the headless `ProjectSession` (the `scripts/dogfood/harness.py` EGL path) on a project with a hand-placed scripted uniform; render an IMAGE at two distinct `t` values and assert the rendered pixels DIFFER (the scripted uniform animated) — and that a deliberately-broken script renders a frozen (non-crashing) frame. **PREREQUISITE (review-flagged): this is INFEASIBLE with the current harness** — `_render_image` defaults `u_time=0.0` (core.py:402) and `harness.render` calls `render_image` with no t override (harness.py:263), so today it can only render at t=0. The scenario therefore DEPENDS on decision 4's tick→render seam being implemented first (the tick is what advances the per-frame `t` the engine writes into `uniform_values`), AND the harness needs a way to render at a chosen `t` (a `harness.render(t=...)` or a tick-then-render entry). Both are listed in Files-touched; the dogfood is a verification of decision 4, run AFTER it lands — not a precondition for it. If decision 4 slips, the fallback verification is the pure-CPU determinism test (same `ctx.t` → same computed value) + the GL-integration test (value reaches the GPU at a chosen `t`).

**`make run` hand-steps (handed to the maintainer — exact script bodies):**
1. `make run`. Pick a node with a `float u_x` uniform (or add `uniform float u_wave;` and use it, e.g. `fs_color = vec4(vec3(u_wave), 1.0);`).
2. Create `nodes/<that-node-id>/scripts/u_wave.py` on disk with body:
   ```
   out.set(0.5 + 0.3 * sin(ctx.t))
   ```
   → the uniform should PULSE between 0.2 and 0.8; the slider should no longer be hand-settable (the engine writes it each frame).
3. Add a `uniform vec2 u_offset;` + `scripts/u_offset.py`:
   ```
   r = 0.4
   out.set(r * cos(ctx.t), r * sin(ctx.t))
   ```
   → the vec2 should ORBIT (temps-then-`out.set` works; no last-expression magic).
4. Break it: edit `scripts/u_wave.py` to `out.set(0.5 +` (syntax error) → the app must NOT crash; `u_wave` FREEZES at its last value; a `ScriptError` is recorded with the user-source line (logged in v1, surfaced in UI at 041). Fix it → it resumes live (hot-reload via the `(path, mtime)` watcher).
5. Export an image/video of the scripted node → the exported media must ANIMATE the scripted uniform identically to the live preview (decision 4; same `t` → same value). This is the live↔export determinism check the maintainer confirms visually.
6. Copilot: ask it to `set_uniform u_wave` on that node → it must be REJECTED with a clear "script-driven, cannot be set" message (decision 5).

## Review sizing

**Proposed: mid → upper end (treat as high-blast-radius for post-impl).** Rationale: it adds a NEW subsystem (the `shaderbox/scripting/` package), TOUCHES the render chokepoint (`Node.render` is read by the new tick contract) AND the export path (`_render_video`/`_render_image` reroute), and crosses the 025 ProjectSession boundary + the copilot `set_uniform` reject. That's more blast radius than a contained feature, but it does NOT rewrite an existing subsystem (it adds a new one beside the core) — so it's not the `ui.py`-split tier.

- **Pre-implementation: 2 review agents** (the mid-feature default, at the upper end): (a) *correctness & design* — the output-object (`out.set`) design genuinely removes blocker 1 by construction (no body rewrite, so no hidden-transform ghost), the `Behavior` abstraction genuinely admits a C backend with no loop change, the 025 boundary is respected by Option A's export seam; (b) *verification & blast-radius* — does the manual-verification list (esp. the headless dogfood + the GL-integration test) catch a real live↔export divergence, does the `set_uniform` reject have a test, are the 041/042/043 deferrals folded with concrete triggers.
- **Post-implementation: 3 review agents in parallel, run as a convergence loop** (high-blast-radius floor): *code correctness* (the `exec`/`compile` seam, the per-tick error catch never escaping, GL-context lifetime in the GL test, the `(path, mtime)` cache not leaking compiled objects), *architecture & conventions* (the `scripting/` package boundary, the coercion-reuse home, the ONE sanctioned suppression added to the allowlist, no imgui/glfw import in the engine), and a *spec-fidelity audit* (every locked decision 1–12 actually landed). **At least one reviewer anchors to a non-self-authored artifact** — the running headless dogfood render output and/or `tests/test_uniform_seed_save.py` as the GL-fixture precedent (`/review-agent-loop`).

## Review history

**Pre-impl review round 1 (2026-06-13, 2 adversarial agents).** Both returned PARTIAL/FAIL; triaged:
- **REJECTED (false positive):** "decision 2 is self-contradictory — the proto rewrites the body." The proto IS outdated (it still does the `__out=(...)` rewrite at `engine.py:122`), but the spec deliberately REPLACED that with `out.set()`. Reviewer A read the proto as the spec. The second reviewer correctly PASSed decision 2. Resolution: the proto is flagged for update (Files-touched) so it stops misleading; decision 2 stands.
- **ACCEPTED + folded:** (1) `out` needs the uniform metadata — made explicit (`UniformOut` holds the live `moderngl.Uniform`; decision 2/7). (2) Export routing is a HIGH-blast multi-caller hot-path touch (Render tab + share scratch + 3 copilot render sites), not MED — re-rated; the worker-thread render is already bridge-marshalled to main, so the tick is safe there (decision 4). (3) Dogfood "two-t" scenario is infeasible on the current harness (`_render_image` defaults t=0) — it now DEPENDS on decision 4 + a `harness.render(t=...)`, run after decision 4 lands, with the pure-CPU/GL tests as fallback (Manual verification §5). (4) Smoke assertion is meaningless until `ui.py` ticks — ordering pinned; engine-correctness claim prefers the unit test (§4). (5) Coercion hoist is LOW-MED (3 import sites incl. 2 test files), not LOW — re-rated. (6) Determinism is scoped to `ctx.t`-pure scripts; integrators diverge by design — acknowledged in decision 1 + tested as expected.
- **Verdict after fixes:** the three would-be blockers are resolved in-spec (no redesign — all were spec-precision gaps, not design faults).

**Pre-impl review round 2 (2026-06-13, 2 adversarial agents, against the patched spec + the updated proto).** Both confirmed all six round-1 fixes RESOLVED, and both independently found ONE new flaw introduced by the round-1 patch wording: decision 4 said "`render()` (or the export loop) invokes the hook," which would DOUBLE-TICK the live frame (ui.py `session.tick` + a hook inside `render()`). One reviewer also flagged the tick SCOPE was unspecified (current node vs all rendered nodes). Both ACCEPTED + fixed: the `on_pre_render` hook fires ONLY from the export loop, NEVER from `Node.render()` (live ticks once via ui.py); tick scope = exactly the nodes the live frame renders (same gate as ui.py:135-141). A residual §4↔Files-touched contradiction (smoke asserting `errors`-empty vs "move correctness to unit test") was reconciled: smoke owns "doesn't crash," the unit test owns correctness. No other new findings; the proto was updated to the `out.set()` shape and re-run (live↔export determinism, `out.set`/no-set/error-as-data all confirmed). **Converged — design sound, ready for implementation.**

**Post-impl review (2026-06-13, 3 adversarial agents — code-correctness / architecture-conventions / spec-fidelity — then a round-2 focused convergence re-check).** Implementation landed in 5 commits (`9924d58`..`c5c2d0a`). All 12 decisions audited COVERED (decision 12's `exec()` suppression resolved faithfully: ruff `S102` isn't in this repo's `select` and pyright basic is clean, so NO marker was needed — outcome recorded in `conventions.md`). THREE real findings, all fixed in `034db20`:
- **`make check` red on the committed tree** — `scripts/dogfood/verify_script_engine.py` failed the pinned ruff-format hook (the format-on-save reflow ran after the commit). Fixed: reformatted.
- **`ui.py` live tick scope inexact** — missed the render gate's `frame_idx==0` render-all clause (a non-current scripted node rendered un-ticked on frame 0 when "render all" is off). Fixed: `renders_all = is_render_all_nodes or frame_idx == 0`. (The companion round-1 claim "ticks the current node under a popup where render skips it" was confirmed a FALSE POSITIVE — the current node's preview render is NOT popup-gated, so ticking it always is correct.)
- **`ctx.uniforms`-integrator export double-tick** — the top-of-frame live `session.tick` advances an integrator by one wall-clock `dt` on the same frame a deferred export runs, poisoning the export's frame-0 start. `ctx.t`-pure scripts (the only v1-GUARANTEED kind) are provably IMMUNE (the export tick fully overwrites). Documented (not code-fixed) as a 042-deferred limitation in `todo.md` — the clean per-export engine-state isolation is 042's state work; building it now is gold-plating deferred scope. **Round-2 verdict: CONVERGED** (all three fixes PASS, no regression, 391 tests green + the dogfood determinism check exits 0; the only residual is the already-filed ruff-pin version drift).

## Plan-lock resolution log (2026-06-13)

The five open questions were resolved by the maintainer at lock; recorded here, folded into the decisions above:

1. **Export-routing seam → Option A** (ProjectSession `render_node_frame` wrapper; `Node` stays engine-ignorant). Decision 4.
2. **Value convention → an OUTPUT OBJECT (`out.set(...)`), NOT a last-expression rewrite.** The maintainer's call: hand the result back via a passed-in object the user writes to, rather than leaving a value "hanging in the air" (a bare trailing expression) or an unexplained `out =` binding. This removes blocker 1 by construction (the body is `exec`'d verbatim — no rewrite, no AST line-remap, no edge cases). `ctx` is the read-only world; `out` is the value sink. Decision 2.
3. **`numpy` → NOT exposed in v1** (math-only). Out of scope (revisit on a real vector-math need).
4. **Shared coercion home → a new leaf `shaderbox/uniform_coerce.py`** (public `coerce_uniform_value` etc.), imported by both `copilot/backend.py` and `scripting/`. Decision 7.
5. **Script watcher → on the script engine** (`scripting/engine.py`), called from `ui.py`. Decision 9.

All low-level naming/shape choices (the exact `UniformOut` API surface, module split granularity) are delegated to implementation, not re-surfaced for sign-off.
