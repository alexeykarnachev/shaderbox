# 078 — the maintainer's walk findings (raw)

The maintainer's own stream of findings from walking the app on 2026-09-04, copied VERBATIM from
`../TODO` at session end. It is a dirty file by his own description — a naked stream of thoughts —
and that is deliberate: the next session's first job is to refine it into `01_spec.md` (one entry
per finding: what was seen, what is decided, what is out of scope) and decompose it into tasks,
BEFORE fixing anything. Do not edit this file; refine beside it.

Two things to keep in view while refining:

- The editor-library items (the `dd` cursor column, the current-line strip after `u`, `l` /
  `->` at the end of a line, the e2e test harness that catches none of them) belong to the
  vendored editor's own session — the maintainer wants a bugs compilation filed for it, with an
  explicit ask for a more robust end-to-end test harness. The rest is ShaderBox's.
- The autocomplete / "shift+k" / formatting cluster is one design question, not five fixes: the
  editor should understand the document's context (script symbols, the uniforms a script
  returns, pass samplers that could be wired), and the maintainer asked for an architecture
  designed first and kept partitioned, and for no half-done stubs — the `u_time`-only suggestion
  is the example of what not to ship.

---

script: the default Docstring is mostly noise, we need to implement a proper "shift+k" on python symbols, including our local stuff like Ctx. The same for the autocomplete (in glsl as well). We should do autocomplete for the local symbols as well, track them or something like that. Also, for the script we should fully support python auto-complete. I don't know how to implement this properly, maybe there is something like pre-compiled python symbols table? I don't want to integrate the full-blown language server, but maybe this is an option as well. But I'm afraid this is too much of the integration, let's check. In any case, the current state is too weak. Also, I want to integrate auto-formatting option (ctrl+shift+I) for glsl and python. Also, when providing auto-complete options in glsl it would be helpful to show the options which script returns. Since we are vendoring this python backend machinery ourselves we can leverage this and implement a really conenient environment: we can parse the ast and find all the return statements and extract the uniforms we return and suggest them as autocomplete in glsl. Also, I suggest to use different text colors in autocomplete suggestions: different colors for the builtin uniforms (u_time, u_resolution and so on) and distinct color for the script's uniforms. Just make sure that all this machinery is carefully abstracted and partitioned, don't scatter all this logic, first design a good architecture which we can easily maintain and generalize. We must utilize the fact that we fully control our environment, the editor should "understand" the context.
One more random finding: i have the following in the editor:
"""
...
uniform float u_time;
uniform float u_aspect;

...
"""

and then under the u_aspect uniform I start to type "uniform vec4 u_" and it suggests me "u_time" why the fuck it suggests me this auto-complete? u_time has no sense here... why not u_aspect? why not anything else? Why do I even see the uniform suggestion here at all? We don't have existing symbols recall at the moment as I understand... Anyway, this is some shitty inconsistent strage behaviour which we need to polish.

One more bug:
If a cursor is standing on a line and I perform "dd" the line disappears, good. But the cursor appears at the beginning of the next line, but not at the same position as it was before. The real vim behaves differently. We have these huge end-to-end tests with a lot of random inputs, lines removal, editing, movement operations and so on. Why don't they catch this simple case? This is the question for the editor session. When you'll file the bugs compilation for it, ask it to implement more robust end-to-end tests which accumulate the possible errors and mistakes quickly. It is obvious that the current testing harness is too weak. Now, next: I undo this line deletion "u" and the line appears. But the current line (gray-ish strip across the current line) stays at the second line, it doesn't update back to the first line.

one more random thought, suggestion on the auto-complete options colors: red-ish for special words and builtin types (continue, if, vec3, etc), blue-ish for builtin uniforms (u_time, u_aspect ...), green for script uniforms... something like that. We'll tune on the spot if needed.

okay, this shit again. I'm typing "vec3 color = u_" and it suggests me "u_time" again. Only this fucking u_time... I don't understand, it looks like a half-done solution, looks like you just slapped a stub somewhere. Please don't do this. You either implement a proper solution or don't slap these half-way done things, it looks aweful, lame and non-professional, this is just a stupid rot everywhere.

another one: in vim, when I hold "->" or "l" and the cursor is at the end of the line, I don't jump to the next line. The cursor stops on the current line.

about the errors in the editor: when the cursor is hovering the errored line it would be useful to highlight this specific error on the errors panel (which is at the bottom of the editor). And maybe show the popup with the error. Or maybe only one of these solutions? We need to visualize it somehow... I'm suggesting the following: we just highlight the error at the bottom. But if we press "f8" - jump to the next error, we also show the popup explicitly; otherwise if we just hover it without pressing f8, we only highlight the error at the bottom.

About the "reset" button which resets the document. My suggestion last time was wrong. Let's better put it inside the Document tab, at the top, where we keep Document name and Canvas, all this stuff. Because this is about a document, not about documentS

next: when adding a new pass, we currently open an input text field where we need to input a name. This input field looks aweful, it is stretched on the whole length of the panel, it has this wierd cross to close it... I have a better suggestion: let's just open the pass settings modal, just add another button "Create" along side with the "Close" button. In this way, we can reuse the same modal and no need to invent any new text input fields. And by the way, we already open the settings modal when we confirm the name, so obviously we can combine these steps.

one more: about glsl autocomplete. It would be helpful to provide autocomplete suggestion for a possible sampler2D which could be wired from another passes.

another one: on the uniform panel, when i select a source of a texture and click this list selector, the options looks confusing, this is just a plain list of stuff:
[
auto (paint)
none
foo
paint
file...
]

maybe we colorize them / group them? something more ergonomic and noticable... the current representation looks boring and confusing...


one more feature: let's also colorize the builtin uniforms names in glsl (blue-ish?) and maybe distinguish them from the passes sampler uniforms? Also, make sure that the mechanism is realiable and robust, i.e when I change the uniform source to a texture, the color should change back to the normal uniform... something like that.

when i want to create an openrouter key by pressing on the button in the copilot widget (if the key is not created yet) the settings window opens, but I need several seconds to find the input field visually. Let's highlight it when we get transfered from the copilot widget. (the same for the telegram key and for the youtube key probably)

one more:
when the copilot is changing my shader for some reason the code color completely fucks up... Also, I noticed that the copilot inserts its first line shifted to the right. When I shift it back, the code color restores back to normal... something strange... seems like two different bugs: the color and this strange offset of the first code insert.

when sending a message to the copilot for some reason the rendering (the main rendering canvas) render the checkerboard insteaf of the black alpha channel. Something gets reset when agent is thinking or what?
