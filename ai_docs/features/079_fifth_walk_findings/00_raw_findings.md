# 079 — the maintainer's fifth walk findings (raw)

The maintainer's own stream from the display checks of 078 on 2026-09-05, copied VERBATIM from
`../TODO`. Do not edit this file; the refined ledger is `01_spec.md` beside it.

---

when triggering help popup (shift+K) and then triggering on some other symbol, the first frame blinks with the first popup, then appears the second one. Happens very quick, seems like a single frame blinking, but it is there. The same flickering happens with error popups (f8) .. looks like some general issue.

let's sort suggestions by their type (color) -- when possible, otherwise alphabetically. Maybe later we'll introduce some more clever sorting, but for now by color then alphabetically.

Also, let's show the current uniform value inside the hint popup (make sure that long array-like uniforms correctly stripped and don't overflow). Also, if it's possible and easy, let's visualize a texture inside sampler2D help snippets. Try not to introduce any whacky work-arounds for this. But probably it is not possible without modifying the editor api. In this case, skip this texture visualization idea, let's not overcomplicate this.

Let's also color output shader variable name into orange-ish color, we probably have a suitible one in our theme. Keep this generalizable across different themes though.

I still don't like the docstrings in python code. Can you research good docstrings style. This looks aweful:
"ctx.t seconds since start; ctx.dt seconds since the previous frame; ctx.frame the frame index; ctx.mouse x, y", all these ";" in one line, full mess. The same is for other occurances, for example: ctx.dt - the help popup is empty. ctx.frame -- is empty. ctx.mouse -- aweful help string, just messy one liner with ";", there is no any discipline in your docstrings at all, refine this properly, make stuff consistent. Check all other possible gaps, I didn't check everything. The same is for "Ctx" itself, one short sentence, awefully disballanced... Complete mess....

autocomplete in python works strangely. I type a number and a dot: "1." and popup appears with python's words: "and, if, in, is"

When a script returns a uniform which is not used in the shader, we have an error which looks like this:\
u_tmp: pass 'main' has no active uniform 'u_tmp' (orphan key)
I'm not sure that we need to treat it as an error. I usually first write something in the script and then use it in the shader later, no need to visualize it as an error.

Also, this errors pane at the bottom of the editor hovers vim's status bar, align this stuff properly.

About the texture source picking list: Still looks messy:
[
auto (main)
none

pass
main

file...
]

none and pass has the same color and style, although "pass" is not even an option, it is just text. Do we even need this "auto (main)", can't we just automatically set what we can and if user wants, he will change. And no need for this "pass" header. Just keep none, pass0, pass1...., file... ; keep none gray-ish, keep passes green-ish and file... just white (the current setup, just without all these empty lines, without (auto) and without "pass" header).

autoformatting on the initial script state does something. (adds empty line somewhere). The script should be formattes in its initial state.

About the "Reset" button for a Document. I don't like the current layout:
Document Name   Canvas
[          ]    [   ] x [   ]  [presets]  Reset
Very messy, it is not clear what this "Reset" about..
Let's do the following: put presets button as a little button on the same line as the "Canvas" title, like so "Canvas [presets]" , but distinguish this button from the "Canvas", probably make this button in a form of a chip or something like that. And the "Reset" button -- align it to the right border of the Document panel. Probably at the same vertical level as "Reset" button (make it red?). Long story short, we need to polish this ui.

One general ui concern: we have this bytton style which looks like a plain text (e.g "add pass", "open", "Reset" and so on), I don't like this style, it looks like a text, the boundaries are not clear, often this blures with other neighboring text and so on. We need to refine it. Maybe we should remove this button style at all and use the "Copilot" style button (button with a border) or something like that... And this issue could be merget with the "Reset" and "presets" chips that I suggested...
Can you craft html with various candidates. Let's emulate the Documents panel with different buttons style. I will pick some and then we will generalize the picked style on our global theme and apply everything. So, don't change anything for now, just prepare html, I will check it first.

about 12. Checkerboard, I still see checkerboard... Maybe it has something to do with this dimming that we add to the whole application when copilot is thinking.... ??? is this some bug in imgui or something like that?
