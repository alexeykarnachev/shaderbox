# 093 — Refinement: the raw ledger

The maintainer's dogfooding of the graph view (092), fed back in rounds and
fixed round by round, the same loop as the earlier walks. Each finding is quoted verbatim and
checked against the code before it is filed; numbered in the order he wrote them, never
renumbered. Batches he reports in one go are one wave each: one gate run, one commit, this
ledger updated in the same commit. A finding too big for a wave becomes its own numbered
feature, and its row here points at it. The spec that groups the waves is `01_spec.md`.

| # | Class | Step | Reported | What the code does (verified) | Landed in |
|---|---|---|---|---|---|
| 1 | UX | the graph view, a wire | "Need to be able to select an edge by clicking on it (in order to delete: by pressing del or by clikcing on the cross which we should render at the center of the bezier line)" | A wire has no hit rect: `_draw_canvas` submits buttons for the background, the nodes and the ports only, so a wire cannot be selected; the only removal today is grabbing the wire off its port and dropping it on empty canvas (092 D12). A bare `Delete` cannot be a registry chord (`commands.chord_needs_modifier` refuses a bare non-F key), so a Delete key on the canvas is a widget-local read gated on the canvas being hovered, and the ✕ on the curve is the discoverable affordance. | wave 1 (f012074) |
| 2 | UX | the graph view, hover | "Higlight elements: nodes, pins, edges on hover." | Nothing changes on hover: `_draw_node` reads only `is_output` / `selected` / `error` / `stale`, the port dots one color, the wires `GRAPH_EDGE` or the dim/error variants; the hit rects know they are hovered but the picture is drawn before them, so a hover cue is a frame late unless the previous frame's hover is kept on the view state. | wave 1 (f012074) |
| 3 | UX | the graph view, a feedback pass | "No need to show the feedback edge from the output to the input of the same node, the edge is overlapped by the node, it looks unpleasant. Instead, draw a little double looped arrow (where you draw \"xN\" at top right of the node card) or something like that." | `_draw_self_loop` draws a cubic from the output dot over the node's top to the `prev` port; the descent to the port crosses the picture's top-left corner (his screenshot, `paint`). The `prev` port itself (double ring, 092 D11) stays as the port; the loop is what goes. | wave 1 (f012074) |
| 4 | UX (design) | the control panel and the Document tab | "I think our \"Document\" tab is too overblown, not enough space for the graph. We need to refine this ui. We have document name input field, we have canvas resolution, then we have this new line and Script [open] stop controls, then We have this \"Passes [trip][graph]\", then new line again... Too much space is wasted... Also, we have this document picker on the left side of the control panel, It has a lot of dead space. Even if a project has a lot of documents, a user rarelly clicks on them, this is wasted space as well... Long story short, we need to re-design this ui, it is not convenient, the graph space is too small, a lot of rarelly used elements everywhere and whatnot." | `ui._draw_app_panel` splits the control panel into the document grid (`width / 2.6`, `draw_document_preview_grid`) and the settings tabs; `tabs/document.draw` stacks the name and canvas row, the Script row, the Passes row and the canvas, each with a `SPACE.MD` gap. The graph gets what is left of a child sized `max(PANEL_CTRL_MINH, region.y)`. A redesign of the panel's composition, not a wave-sized fix: its own feature with a mock, sketched first. | — (delegated to its own feature) |
| 5 | DEFECT? (mine) | the graph view, a long wire | (my observation on his screenshot: `paint -> raymarch.u_paint` spans three ranks and should ride the bus under the row, and no bus line is visible) | To verify in the wave: `_fit` frames the nodes' bounding box, not the bus below it, so a bus wire can sit below the fitted view; and the bus's y is the bottom of the visible nodes plus `GRAPH_BUS_CLEAR`, which the fit's margin does not cover. | wave 1 (f012074) |
| 6 | UX | the Document tab | "Put open graph button to the document panel (new the script button)" (voice: NEAR the script button) | Wave 1 put the graph's `open` on the Passes row (`tabs/document.py::_draw_passes`, T5), two rows below the Script row's `open` in `_draw_entry_points`; he wants it beside the Script one. | wave 2 (1974aa0) |
| 7 | UX | restart | "the opened graph tab is not remembered after the application restart (probably other tabs as well. We should preserve the opened tab after restart)." | `App._init` reopens only the current document's shader tab (`ensure_shader_tab`); `UIAppState` persists no tab list and `App.save` mirrors layout prefs only, so every other tab -- script, graph, lib, other passes -- is lost on restart. | wave 2 (1974aa0) |
| 8 | UX | the graph tab, a feedback pass | "the icon, representing a node's self feedback loop is ugly, remove it. Let's for now keep only this \"xN\" tooltip." | `_draw_node` draws `_draw_feedback_glyph` (two open rings, wave 1 G8) in the badge row of any node with a `prev` port; the `xN` pill is `_draw_badge`. The `prev` port's double ring (092 D11) is a port, not the glyph, and stays. | wave 2 (1974aa0) |
| 9 | UX (design) | the graph tab, an input pin | "Let's disallow dragging by the input pin. Currently, I can drag an input pin and detouch an edge. I don't like this behavior. Let's detach the edge explicitly by clicking the cross icon. Although, I'm not sure about this. Can you check how do other softwares do this? I specifically don't like that when I starting to drag the edge, the yellow ghost appears which connect the input pin and the mouse cursor, so it is kinda not obvious that the mouse button release will lead to the edge removal... we need to check how do others do this..." | The port-press branch of `_draw_canvas` starts a `WireDrag(grabbed=(owner, sampler))` at the producer end when the port is `wired`, the in-flight wire draws in `ACCENT_PRIMARY` (the yellow ghost), and `_drop` unwires on an empty drop (092 D12, G14 "re-plug"). `research/B_mouse_controls.md` Q6-Q7: imgui-node-editor, litegraph (default), xyflow (`reconnectable`) and Blender detach by dragging the wire off the input with a ghost to the cursor; imnodes offers the same behind an opt-in per-pin flag; Houdini is click-based (click the input, then click empty space); Unreal adds Alt+click; ImNodeFlow has no detach gesture. Among the references that draw a wire from a filled input, the schema he dislikes is the convergent one. | wave 2 (1974aa0) |
| 10 | UX | the graph tab, a card | "Let's make the render previews a little bit larger. Decrease the gap between the canvas previews and the node's card border by 50% of the current gap. (the borders look too fat at the current moment)" | `_thumb_rect` centres a `GRAPH_THUMB = 96` picture in the `GRAPH_NODE_W = 136` card (20px each side) at `GRAPH_PAD = 8` from the top; the border itself is 1px (`_draw_node`), so the fat look is the margin. | wave 2 (1974aa0) |
| 11 | UX | the graph tab, a card | "And let's add alittle bit of a margin at the top: between the last uniform name and the bottom border, unless the gap is already the same as the left and right ones... it seems like it a little bit smaller." (the BOTTOM margin) | `node_size` ends a card at `GRAPH_PORT_TOP + n * GRAPH_PORT_ROW` with no bottom pad: the last label's text bottom sits about 3px above the border, against the label's 10px left inset (`2 * GRAPH_PORT_R + 2` in `_draw_node`). | wave 2 (1974aa0) |
| 12 | UX | the graph tab, a card | "increase the top gap as well. make it the same as for other sides. I mean the gap between the node's card content and the border." | `_thumb_rect` put the picture `GRAPH_PAD = 4` under the card's top and centered it in the width, which left 10 on each side (`(136 - 116) / 2`); the top was the one gap wave 2 did not lift to the sides' value. | wave 3 |
| 13 | UX (design) | a pass tile, a node | "let's not open the shader code when clicking on the pass." | The strip's tile click ran `pick_pass` (= `ensure_shader_tab` + `choose_output`), so a click in the Document tab activated the shader tab and evicted the graph tab from the editor pane; the uniforms row's source preview did the same by its own comment ("does what the strip's tile does"); on the canvas the double-click kept `pick_pass` (S15). | wave 3 |
| 14 | DEFECT | the node's context menu | "I can't open shader from the context menu of node." | `pass_menu_items` (shared by the tile and the node) had Settings / Delete / Leave group and no open item; the only ways to a pass's shader were the tile click and the node double-click, both of which finding 13 removes. | wave 3 |
| 15 | UX (design) | a pass tile | "let's remove these settings and remove buttons from the pass preview in the \"Document\" tab. Move these actions into the context menu." | `_draw_pass_tile` drew `tune_icon_button` as the tile's overlay (the gear) and armed `preview_cell`'s corner ✕ through `App.pass_delete_armed` (a two-step in-cell Delete?); both verbs were already on the tile's context menu, so the overlays were the second affordance for the same thing (/imgui-ui §7.4). | wave 3 |
| 16 | UX (design) | the graph tab, the `prev` port | "let's not allow to connect u_prev pin with anything... This is special uniform. Actually, let's not show it at all. Probably later we'll implement the uniforms extention button which will show all the node's uniforms, but for now, let's just should the expclicit inputs only." | `node_ports` appended every self-read as a `Port(kind="prev")` drawn as a double ring (092 D1, D11) and a drop target the planner accepted (`refuse_drop` allows feedback); a feedback read is not an input from another pass. | wave 3 |
| 17 | UX (design) | every menu, popup, modal | "let's review and refine all our context menus, popups, setting modals and so on... Currently, this is a complete mess ... collect all the menus, config popups, context menus, all this stuff into a single md file ... run 2-3 opus agents on top of it, review that nothing is missed. Then, review this holistically, define gaps, propose improvements ... good coverage, a very convenient ui/ux ... don't overblow the state ... convert this file into the actual spec ... launch opus implementator ... post-impl review ... this refinement should also include the code itself ... the same context menu for the node and for the pass ... perform all neccessary refactorings and unifications" | Not a wave: an inventory of every menu, popup and modal in the app, reviewed for completeness, then a design pass and a spec, then the feature flow. Runs inside this feature's directory as `04_menus_inventory.md` -> `05_menus_spec.md` (his instruction: keep it here). Findings 14 and 15 are fixed requirements of that spec, not open questions. | the menus wave: f045eef + d6bc425 (post-implementation round), 505c992 + this commit (the command system designed as one, `06_command_system.md`) |
| 18 | UX (design) | the bar's Document menu, every destructive verb | "I'm not sure about these reset document and delete options. Why do we need this expandable options? Why not just show modal popup?" and "Don't we have a unite generalizable modal popups mechanism which we can re-use? If not, let's refine this aspect of the code." | Both halves held. The submenu (the menus wave's M5) opens on HOVER, so the confirm is reachable without a decision; it carries no consequence text and no chord hint (`command_menu_item` drew a `begin_menu` for a spec with a `confirm_label`, which has no hint slot), and it is a shape no desktop app confirms with. And the mechanism was half there: `ui_primitives.modal_window` owned the imgui chrome, but the ROSTER was hand-maintained in five places (a `PopupState` enum on `App`, one `open_*` each, a `close_popup` dispatching by hand, eight `draw_*` calls listed in `ui.py`, a table in the chrome gate) -- and the copilot's revert confirm sat outside all of it with its own `copilot_revert_target` field. The conventions bullet already warned that forgetting one of the five is INVISIBLE at runtime. | wave 4 (`07_modal_registry_spec.md`) |
| 19 | UX | the graph tab, the canvas | "remove the border of the graph canvas. there are too many borders there close to each other. the middle vertical borders of the screen create a clutter: the graph canvas border, the whole outer border of the tab, and the documents pane on the right side... I'm not sure though, should we remove the canvas border or the whole outer tab border? Let's find a clean solution for this..." | `widgets/pass_graph.py::draw` opened `##pass_graph` with `ChildFlags_.borders` INSIDE the editor pane's own bordered `code_editor` child, with the app panel's frame to the right; the code-editor tab draws no bordered child of its own inside the pane, so the graph was the one tab carrying a second frame. The canvas child's border goes and the pane's stays (it separates the pane from the splitter and the app panel); `always_use_window_padding` replaces the padding `borders` implied, so the canvas rect (`get_cursor_screen_pos` + `get_content_region_avail`) is unchanged. | wave 5 |
| 20 | DEFECT + UX (design) | the Help modal, every modal's action row | "on the help panel there are [insert at caret] and [close] buttons at the bottom... wtf the insert at caret is doing here? Also, these buttons are out of the border and they cause the vertical scroll out of nowhere, fix this. And once again, we must generalize this: there MUST be a generalized mechanism for placing buttons (e.g close) at the bottom of settings, help menu and all this stuff... (fix only the layout and the ui now). And rename this, 'help panel' is an ugly name. Call it documentation or just 'help' although the full menu is called 'help' already, pick the better name" | `popups/help.py::_draw_body` reserved `list_h = -get_frame_height_with_spacing()` for the action row and then drew `dummy(SPACE.MD)` + the row, which is taller -- the overflow was a scrollbar. Every other modal computed its own reservation by hand (`projects.py` two rows, `lib_picker` a `body_h`, `examples` fixed math), so the two numbers could disagree per modal. `ui_primitives` gains the trio `modal_footer_height` / `modal_content` / `modal_footer` and every body adopts it. `Insert at caret` goes: the modal is a reading surface, so `HelpSection.insertable`, `_insert_target_ok` and the disabled tooltip go with it (`App.insert_text_at_caret` stays -- the lib picker inserts through it). The name is `Documentation`: the command's label, the modal's label, the modal's title. | wave 5 |
| 21 | UX | the graph canvas menu | "rename 'fit' option in the canvas menu into 'center' or something like this... 'fit' is too vague" | `pass_graph._canvas_menu`'s item was `Fit` (it clears `view.fitted`, which zooms AND pans to the whole graph). `Frame all` is the verb graph tools use for that; `center` would promise a pan with no zoom. | wave 5 |
| 22 | UX (design) | the documents grid, the lib tree | "remove this gray text: 'right-click for actions' from the documents viewer. Which actions? What is this even about? We shouldn't put these hints everywhere, everything must be intuitive, without all these hints here and there." | The dim `Right-click for actions` caption sat in two places -- `widgets/document_grid.py` beside `New document` and `popups/lib_picker/__init__.py` above the tree. Both go, REVERSING the menus wave's M6/M12 and the conventions bullet that a hint sits over a modal's or a panel's list: a right-click menu is discoverable by convention. | wave 5 |
| 23 | UX (design) | a document tile's context menu | "context menu by clicking RMB on the document preview: there is an option \"Open\" -- what exactly are you openeing here? This is an air, it does nothing useful, this is fucking noise option, remove it. We can simply click LMB on the document in order to \"open\" it." | `document_menu_items` opened with `Open` -> `app.select_document`, the verb the tile's own click already runs. Gone; the menu is `Open folder` / `Delete`. `06_command_system.md`'s non-command list names the new pair. | wave 6 |
| 24 | UX (design) | the confirm modal, every request | "\"delete\" option show a modal window and there is a text here: \"Nothing in the app brings it back\" -- remove this fucking noise, this is the internal information, also, we will fix it in the future, no need to explicitly tell about this crap..." | `ConfirmRequest.line` carried one sentence of consequence under every title (seven builders: the revert, the pass delete, the document delete and reset, the chat clear, the lib tree's two deletes). The field is deleted with all seven lines: a confirm is the question naming the verb and its target, and the verb on the button. | wave 6 |
| 25 | UX | the Document tab, Fixed presets | "add 4:3 preset resolutions to the \"presets\"" | `_canvas_presets` derives its list from three sources: the square sizes, `MENU_SHAPES` through the shape table, and every bound texture's size -- a 4:3 entry belongs in the shape vocabulary, not in the list. `RenderShape` gains `STANDARD_960/1440/1920` (4:3 at the same 1280/1920/2560 longest-edge ladder the other groups use, so a tier name means one size across aspects; a test pins the shared ladder), which the Fixed combo, the Share combo and the copilot's render tools all pick up. | wave 6 |
| 26 | DEFECT | the script editor's Python completion | "Auto-complete for objects and classes in python contains \"mro\" -- wtf? we don't need this. Also, disable strings autocomplete suggestions, I don't need these `\"uv.lock\"` and `\"upload-itch.sh\"` suggestions inside the python script at all..." | Measured on jedi 0.20: `A.` on a class offers `mro` with `full_name` `builtins.type.mro` (the metaclass protocol; every other `type` member starts with `_` and was already dropped), and a caret inside a string literal gets the working directory as FILE PATHS (`"u` -> `uv.lock"`, `upload-itch.sh"`; `""` -> 38 entries), since jedi completes a literal as a path. Two rules in `intel/python.py`: a caret inside a string (the text up to it tokenizes to an unterminated literal) gets no candidates at all, which also keeps the injected API names out of a string; and a candidate whose `full_name` is under `builtins.type.` is skipped. | wave 6 |
| 27 | DEFECT | the vim keymap, the view-only scroll commands | "\"zz\" -- screen centering hotkey doesn't work in vim editor mode (or at least it doesn't work as in real vim)... as well as ctrl+E, ctrl+Y ... needs to be fixed, I need those 3 working properly as in real nvim" | The editor library's, measured here through the host's own call order (feed -> `take_scroll_request` -> layout; the host follows the cursor only when it moved outside the view) and through the app's frame loop: on a 200-line buffer all three work (zz -> 92 for cursor 99 at 15 rows, Ctrl+E/Y step one). What breaks is the clamp to `scroll_max`: on a 40-line buffer at 15 rows zz gives 25 = scroll_max where nvim gives 27, and on a buffer shorter than the view (a typical shader against his 62-row pane) all three request 0 and do nothing -- the `vim_coverage.md ## Scrolling` divergence he has now overruled. Second, thirteen Ctrl+E from scroll 85 / cursor 99 leave the cursor above the view at 105; nvim drags it to the first visible line. Both sent to the editor session as this wave's one editor item; it landed upstream at `5a56ccf` (the view runs to the last line at the top row, one rule for `ed_scroll_max`, the wheel and every jump; Ctrl+E/Y drag the cursor onto the edge row keeping curswant; zz leaves `(rows-1)/2` above, one row up on even heights), re-vendored here with no export delta (108 either side by `nm -D`). | wave 6 (re-vendor 5a56ccf) |
| 28 | DEFECT | the throttle, a document's script | "when we throttle a document the script doesn't work as intended. I have a drawing script which simply returns current and previous mouse positions to draw a line between them. And I see gaps between lines... Something is not synched properly..." | 090 D8 ticked the script over the full set at the UI rate while a throttled document rendered every k-th frame, and the viewer re-based `mouse.prev` every UI frame: each render saw one frame's worth of stroke and the k-1 frames between renders were the gap. Now a tick IS a render: step 7 ticks the documents `_rendering_this_frame` names, by the render block's own three-way rule and the plan's gate; `session.tick` derives each document's `dt` (document time since its previous tick, kept on `Document.script_time`) and `frame` (its tick count, `Document.script_frame`, cleared by `reset`); and the cursor's previous position is an ANCHOR the tick re-bases at the position it saw, which the viewer's sampler carries across the skipped frames (a re-entry still starts a new stroke). REVERSES 090 D8; the 090 spec carries the pointer. | wave 6 |
| 29 | UX | the editor tab row | "currently if we have only one pass, the tab in the editor shows \"Doc name (shader)\", let's not make a special case of this. Just show the proper pass name (i.e not the \"shader\")." | `tab_label` kept `(shader)` for a single-pass document (065's call: nothing to disambiguate) and named the pass only past one. One rule now: a shader tab is `<document> (<pass>)`, taken from the tab's path. | wave 6 |
| 30 | DEFECT + UX (design) | the confirm modal, every modal's size | "\"Reset\" button's modal popup first appearance is with scrollbar ... the buttons overflow the bottom edge... THESE SIMPLE MODAL WINDOWS (where only a single confirmation question is present, without any options like settings) MUST BE NOT RESIZABLE AND THE SIZE MUST AUTO-FIT!!!!" | The confirm row seeded `(380, 0)` once with `first_use_ever`, so imgui.ini's persisted `Size=381,132` under its label won on every open; a title that wraps needs more, and the footer went under a scrollbar. Measured headless: a RESIZABLE confirm against a 60px entry overflows by 32px on every frame after the first. Sizing is now ONE declaration on the registry row, `Modal.sizing: ModalSizing` -- RESIZABLE (seed once, a resize persists), FIXED (every frame, no handle; Examples) and AUTO (width pinned by a constraint, height from the content up to the display, no handle, no saved settings; the confirm and the pass-settings modal, whose hand-rolled constraint hook and `always_auto_resize` flag moved into the primitive with `Modal.before` and `fixed_size` deleted). The chrome gate pins the choice to the body's shape: a row whose bodies and named `size` function lay out no content region is AUTO, and only such a row is; the footer gate opens each AUTO modal against a stale 60px ini entry and reads zero overflow on every frame. | wave 6 |
| 31 | DEFECT | the documents grid, a tile's click | "a shader still auto-opens when I click on a document on the documents panel. We decided to turn off this auto-openning, but it still happens." | W3-3 (finding 13) took the shader open off the PASS click; the DOCUMENT switch still had it: `App._on_current_document_changed` called `ensure_shader_tab(new_id)` on every change of the current document, so the tile's click (`select_document`) opened and activated the new document's shader tab. The call is gone; a switch leaves the editor's tabs as they were. The two places a shader tab still opens by itself are not clicks and stay explicit: a document CREATED from an example opens its shader (it is what the user is about to edit), and a project opening with no restored tabs opens its current document's. | wave 7 |
| 32 | DEFECT | the viewer, the control panel under it | "the vertical size of the rendering canvas must be fixed. It is still jumping when I change the resolution from 1:1 to 16:9. There must not be any jitters and whatnot in the application. The ui must be stable..." | `_draw_document_image` fitted the picture into the room above the control panel and returned the PICTURE's height, and `_draw_app_panel` anchored the control panel at `cursor + image_height`: a 16:9 picture is shorter than a 1:1 one at the same width, so the panel climbed by the difference on every aspect change. The viewer is now a BOX (`ViewerGeometry`: the panel's width, the height left over the control panel, the same for every document and for none) with the picture fitted and centered inside it; the control panel anchors at the box's bottom, the FPS chip and the channel chip at the picture. `viewer_region` stays the picture's size, which is what an Auto document renders at. Pinned by a frame test: the control panel's y is equal at 1:1 and 16:9 while the picture's height differs. | wave 7 |
| 33 | UX (design) | the control panel, the whole header | "На варианте Б, да, собственно, как и на варианте А, по-прежнему очень много пустого места. Вариант Б его просто перемещают в другую сторону... Давай начнем с базовых безопасных вещей: контекстное меню, reset кнопка, и так далее." | Both mock options move the dead space rather than remove it: A frees ~96px of header but leaves the grid's lower two thirds empty, and B trades that for a settings column wider than its content. So the panel's COMPOSITION stays open and the wave takes only what is safe and independent of it -- the verbs that already exist as commands leaving the panel, and the canvas control folding into one. | wave 8 |
| 34 | UX (design) | the Script / Graph rows, the Reset button | "я думаю что этот... не нужно делать отдельную внешнюю кнопку... Кроме этого... Также, я не люблю эту красную кнопку Reset. Она выглядит страшно, но на самом деле мало что делает." | `OPEN_SCRIPT` (Alt+R), `OPEN_GRAPH` (Alt+G) and `RESET_DOCUMENT` (F6) were each already a registered command with a label, a chord and a menu category, so the panel rows were a THIRD affordance for a verb that had two. Reset also wore `danger_button`, the tier for verbs that destroy work on disk, while it restarts histories, the clock, the script and videos -- all of which come back by running the document again. All three leave the panel for the document tile's context menu, which gains them beside Open folder and Delete; the script's play/stop stays, being a live state rather than a verb. | wave 8 |
| 35 | UX (design) | the canvas control | "этот auto режим для разрешения вообще можно сделать как отдельная опция в списке пресетов, не нужно делать отдельную внешнюю кнопку для него... Наш пресет список отсортирован по aspect. Просто в начале каждого аспекта будет auto опция." | The row carried a mode segment (`Auto | Fixed`) beside a control that CHANGED MEANING under it -- a pixel pair under Fixed, a row of ratio chips plus two custom fields under Auto. The presets list was already grouped by aspect (squares, then the 9:16 / 16:9 / 4:3 shapes), so Auto becomes the first row of each group: one pick now names the mode and its value together. 090 D1's `Auto | Fixed` MODEL is untouched; only the widget changes. | wave 8 |
