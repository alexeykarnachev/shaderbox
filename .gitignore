__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

.venv

.env

trash/
exporter_scratch/
tg_state.json

projects/*/media/
projects/*/renders/
projects/*/copilot/archive/
projects/*/copilot/checkpoints/

# shader-lab scratch: iterative-experiment projects live here, gitignored. A worthwhile
# one is promoted by hand (move out of _lab/ + git add) at the end of a session.
projects/_lab/

# Dogfood run artifacts (project dirs, data dir, JSON dumps, traces, PNGs) — regenerable junk
scripts/dogfood/runs/

itch-config

imgui.ini
tmp.mp4
ai_docs/_*.md

# Local design-handoff scratch (feature 005)
handoff/
handoff_flat/
shaderbox-design-handoff-*.tar.gz

# Playwright MCP browser-session artifacts
.playwright-mcp/

# Claude Code runtime state (the scheduler lock under the tracked .claude/ dir)
.claude/scheduled_tasks.lock
tabbar_spike.ini
scripts/agent_hub/site/
.claude/worktrees/

# Dogfooding station (feature 075): the store -- one dir per experiment holding its event log, the
# renders and the generated pages -- is local, like the harness's run dirs. The record lives on the
# box that ran it; `uv run python -m dogfood.report.build` regenerates the pages from the log.
dogfood/index.html
dogfood/runs/
