# Vim keymap — coverage checklist

Enumerated from vim's own documentation (`:help motion.txt`, 1403 lines), not
written from memory. Each motion's exclusive/inclusive/linewise class is taken
from the doc text, because that class is what makes operators correct: `dw` and
`de` differ only in it.

Runtime docs resolve via `nvim_get_runtime_file` — a hardcoded path is wrong
because a distro AppImage mounts them at an ephemeral location that disappears
when that nvim process exits. To read the file, copy it out from inside the same
nvim session that resolves it.

Expected state for every case below comes from `tools/vim_oracle.sh`, never from
recollection. Harness A covers text/cursor/mode; undo granularity requires
harness B.

Status: `x` implemented and oracle-checked, ` ` not yet.

The case tables in `src/motion_test.odin` (single-line) and
`src/motion_ml_test.odin` (multi-line) hold nvim's own answers and are
regenerated by `make motions` from `tools/*_cases.tsv` — 3634 cases at the time
of writing, across motions, operators, text objects and visual mode. That count
is what `tools/check.sh` gates, and it sums the four files it names:
`motion_test`, `motion_ml_test`, `textobject_test` and `operator_test`. The
end-to-end cases are deliberately outside it, so the number will not match a
count of every `keys =` line in `src/`.

**Every case runs in one nvim.** Spawning a process per case costs ~44 ms of
startup against microseconds of real work, which made a full sweep take over two
minutes; `tools/vim_oracle_batch.lua` reuses a single instance and regenerates
everything in under a second. Two isolation leaks had to be closed for that to
be sound, and both produced plausible-looking wrong answers rather than errors:
loading the buffer is itself an undoable change (so `xxxu` undid the *setup*
until the load was made non-undoable with `undolevels = -1`), and a fresh buffer
per case accumulates swap files until nvim aborts with `E326` partway through.
Harness B still spawns its own nvim — it needs a real tty and is only used for
undo granularity.

The tests drive the **shipped dispatcher** (`src/keymap*.odin`) through
`feed_keys`, not a runner written alongside them. A test-local runner can agree
with nvim while the real dispatcher disagrees, which is the single failure this
harness exists to prevent. Multi-line cases exist as a separate corpus because a
single-line buffer cannot exercise `j`/`k`/`G`/`gg` at all, and a counted `$` has
no line to count to.

Two behaviors that must be measured rather than recalled, both found this way:

- **nvim opens a file with the cursor on the first non-blank of line 1**, not at
  column 0. Starting a test at column 0 makes every `w` case on an indented line
  disagree, and "fixing" the motion to match invents a blank-start rule vim does
  not have.
- **`'startofline'` is off by default in neovim** (it is on in vim), so `G`,
  `gg` and `<C-Home>` preserve the current column instead of moving to the first
  non-blank. On a line of nothing but blanks the first non-blank is its LAST
  character, not the newline past it -- visual mode and every operator read the
  raw position, which `jv^d` on `"aa\n    \nbb"` exposed (`buffer_first_non_blank`).
  `+`, `-`, `_` and `<CR>` go to the first non-blank regardless, and
  make it the DESIRED column: `$_j` on `"aa\n    bbbb"` lands on column 0 of
  the next line and `$_<<` leaves the caret on column 0, where a `_` that kept
  the `$` sentinel put both at the line's end.

## Left-right motions (`*left-right-motions*`)

- [x] `h` `<Left>` `CTRL-H` `<BS>` — [count] left, exclusive. `<BS>` WRAPS onto
  the previous line's last character where `h` stops: `'whichwrap'` is `"b,s"`
  in neovim, which names `<BS>` and `<Space>` and neither letter nor arrow, so
  `j<BS>` on `"ab\ncd"` lands on the `b`. Under a delete or change the wrap
  lands on the newline itself so that is what goes — `jd<BS>` gives `"abcd"` —
  while a yank of the same wrap takes only the `b`, and in a selection the wrap
  lands on the newline too (`jv<BS>d` gives `"abd"`). An `h` that cannot move
  fails and drops the fed sequence (`jhx` deletes nothing) UNLESS an operator
  is pending: `dhx` at column 0 deletes nothing and the `x` runs.
- [x] `l` `<Right>` `<Space>` — [count] right, exclusive; fails only when it
  cannot move at all and no operator is pending (`$lx` leaves the text alone,
  `3lx` from column 0 moves two and deletes, `$dl` deletes the last character).
  `<Space>` WRAPS onto the next line, an empty line counting as one step:
  `$<Space>` on `"ab\ncd"` lands on the `c`, `$2<Space>` on `"ab\n\n"` on the
  last line. In a selection the newline is a step of its own and the wrap
  happens from there (`$v<Space><Space>d` on `"ab\ncd"` gives `"ad"`). Under an
  operator the last character is a step taken INCLUSIVELY: `$d<Space>` deletes
  the `b`, `$d2<Space>` the `b` and the break.
- [x] `0` — first character of the line, exclusive
- [x] `^` — first non-blank, exclusive, count ignored
- [x] `$` `<End>` — end of line, inclusive; [count] lines down
- [x] `g_` — last non-blank, inclusive
- [x] `g0` `g<Home>` — first character of the SCREEN line, exclusive
- [x] `g^` — first non-blank of the SCREEN line, exclusive
- [x] `g$` `g<End>` — last character of the SCREEN line, inclusive

Screen-line motions resolve against the PREVIOUS frame's `Layout_Result`, since
layer 1 owns cursor mutation but is denied font metrics. Verified at the
configuration the design cites — a 50-character line in a 20-column window with
the cursor at column 25 gives `Home`→1, `End`→50, `g0`→21, `g$`→40.

When that geometry is absent (the first frame) or known-wrong (a stale frame on
a line containing tabs, which has no arithmetic path at all), the command is
**deferred** rather than resolved against zeros or approximated.

With wrap off every buffer line is one screen line, so `g0`/`g$` collapse to
`0`/`$`. That falls out of the mapping rather than being a special case — the
wrap table itself says one row per line.
- [ ] `gm` — middle of the screen line
- [ ] `gM` — middle of the line by character count
- [x] `|` (`bar`) — to column [count], exclusive
- [x] `f{char}` — right to [count]'th occurrence, inclusive
- [x] `F{char}` — left to [count]'th occurrence, exclusive
- [x] `t{char}` — till before [count]'th occurrence, inclusive
- [x] `T{char}` — till after [count]'th occurrence, exclusive
- [x] `;` — repeat last `f`/`F`/`t`/`T`
- [x] `,` — repeat last `f`/`F`/`t`/`T` in the opposite direction

`0` vs `<Home>`: `<Home>` works like `1|` and differs from `0` when the line
starts with a tab. `g0`/`g$` are SCREEN-line ends while `0`/`$` are BUFFER-line
ends — the `g` prefix means "screen line" across both axes.

## Up-down motions (`*up-down-motions*`)

- [x] `k` `<Up>` `CTRL-P` — [count] lines upward, linewise
- [x] `j` `<Down>` `CTRL-J` `<NL>` `CTRL-N` — [count] lines downward, linewise
- [x] `gk` `g<Up>` — [count] SCREEN lines upward, exclusive
- [x] `gj` `g<Down>` — [count] SCREEN lines downward, exclusive
- [x] `x` `X` `s` `D` `C` — delete/change shorthands
- [x] `i` `a` `I` `A` `o` `O` — insert entry
- [x] `v` `V` — visual / visual-line
- [x] `u` `CTRL-R` — undo tree navigation
- [ ] `U` — undo all latest changes on one line; absent
- [x] `-` — [count] lines upward, on the first non-blank, linewise
- [x] `+` `CTRL-M` `<CR>` — [count] lines downward, first non-blank, linewise
- [x] `_` — [count]-1 lines downward, first non-blank, linewise
- [x] `G` — line [count], default last line, linewise
- [x] `gg` — line [count], default first line, linewise
- [ ] `H` `M` `L` — window-relative lines; absent, the core has no window
- [ ] `<C-End>` `<C-Home>` — buffer end/start
- [ ] `N%` — to N percent of the file, linewise
- [ ] `:[range]` `:go` `:goto` `go` — ex-adjacent, out of scope

`j`/`k` move by BUFFER line, `gj`/`gk` by SCREEN line. Both must exist.

`g^` leaves the desired column at the screen line's START, not on the
first non-blank it lands on -- vim sets it before walking the blanks and
never after -- so `g^` then `j` reaches column 0 of the line below where
`^` then `j` reaches column 8 under an eight-space indent.

**The screen-line family is closed under an operator too.** `gj`/`gk`
consumed by an operator are observable only in the RANGE, which no caret-only
case pressed: `5|dgj` clamps DOWN to the target line's last character, `$dgj`
takes the newline of an empty line the step lands on, and `j$dgk` runs
INCLUSIVE upward from the target line's last character through the end of the
caret's own line. Their seeds carry a following operator so the range is what
is compared. The caret cases closed by table: the over-range count that MOVES the caret AND fails
(`Motion_Result.move_on_fail`, below), the count `g$` ignored, and the
desired column carried ACROSS a line too short for it — `5|gjgj` on
`"one\n\nthree"` lands on column 4 of `three`, as `5|gjj` does, so gj/gk keep
the display column they aimed at rather than the one the caret was clamped
to, and `$gj` keeps its end-of-line sentinel. `tools/fuzz_diff.py`'s KNOWN set
is the machine-readable list of what is open, and it is empty.

They are also the one family the headless corpus cannot pin: `gj` and its
siblings resolve against the last `ed_layout`, and `src/*_test.odin` has none,
so a row there can never pass. `tools/fuzz_diff.py` lays out and is the only
instrument that can check them.

In visual mode the cursor may rest ON the newline, one past the last character,
and that is how a line break gets selected: `vll` on `"ab\ncd"` puts the cursor
at column 2 and `d` then leaves `"cd"`. `l` there fails, `j` keeps the column
past the end of the next line, and leaving visual mode -- Escape, or the `v`/`V`
toggle -- clamps the cursor back onto the last character. Switching between `v`
and `V` keeps the anchor; only entering from normal mode sets it.

## Word motions (`*word-motions*`)

- [x] `w` `<S-Right>` — [count] words forward, exclusive
- [x] `W` `<C-Right>` — [count] WORDs forward, exclusive
- [x] `e` — forward to word end, inclusive
- [x] `E` — forward to WORD end, inclusive
- [x] `b` `<S-Left>` — [count] words backward, exclusive
- [x] `B` `<C-Left>` — [count] WORDs backward, exclusive
- [x] `ge` — backward to word end, inclusive
- [x] `gE` — backward to WORD end, inclusive

A *word* is a sequence of letters/digits/underscores, or a sequence of other
non-blank characters, separated by whitespace. A *WORD* is any sequence of
non-blank characters. The distinction is the whole reason both sets exist.

## Text object motions (`*object-motions*`)

- [ ] `(` `)` — sentences
- [x] `{` `}` — [count] paragraphs backward / forward, exclusive
- [ ] `[[` `]]` `][` `[]` — sections

Sentences and sections are out of scope; each needs its own boundary rules.

`{` and `}` transcribe nvim's `findpar()`, and three of its rules contradict
intuition, each measured:

- A paragraph boundary is an EMPTY line. A line holding only spaces or tabs is
  not one: on `"one\ntwo\n\nthree\n  \nfour\nfive"`, `}` stops at line 3
  and `}}` runs to the end of the buffer, past the whitespace-only line. A form
  feed in column 1 and a line opening with one of the nroff macros in nvim's
  default `'paragraphs'`/`'sections'` (`.PP`, `.SH`, ...) are boundaries too.
  The line the motion starts on never counts, even when it is one.
- Reaching the end of the buffer lands on the LAST CHARACTER and makes the
  motion inclusive, so `d}` on the final paragraph takes it whole and leaves no
  empty line; `}` on the last line stays put, and `d}` there deletes one
  character.
- A count that runs off either end FAILS rather than clamping: `3}` on
  `"a\n\nb"` leaves the cursor where it was and `d3}` deletes nothing, while
  `2}` on the same text lands on `b`, because the count is spent at the moment
  the edge is reached.

## Text objects (`*text-objects*`)

`iw` selects the run under the cursor — a word *or* a whitespace run, since white
space between words counts as an object of its own. A newline is not a unit of
its own, though: `d2iw` on `"one\ntwo three"` removes `"one\ntwo"`, crossing the
line break within one step rather than spending one on it.

A bracket object does not require the cursor to already be inside the pair.
With the cursor before it on the same line vim still finds it — `di(` on
`"foo(bar baz)qux"` with the cursor on the `f` yields `"foo()qux"` — so the
search goes outward first and then forward to the next pair opening on the line.

- [x] `aw` `iw` `aW` `iW` — word / WORD. `iw` on an EMPTY LAST LINE is vim's
  zero-width object backed up one character: the newline and the character
  before it (`diw` there on `"ab\ncd\n"` gives `"ab\nc"`), or the newline
  alone when the line above is empty, and delete's own promotion then takes
  both lines (`"ab\n\n"` gives `"ab"`). That promotion is the OPERATOR's
  rule, so it reaches an object as it reaches a motion.
- [ ] `as` `is` — sentence
- [ ] `ap` `ip` — paragraph. `tools/operator_cases.tsv` already carries nvim's
  answer for `>ip`, and the operator runner counts it as the one case it
  cannot dispatch (`SKIPPED_OP_CASES`), so implementing these is what lowers
  that number to zero rather than a search for what is missing.
- [x] `a(` `i(` `a[` `i[` `a{` `i{` `a<` `i<` `ab` `aB` — bracket blocks
- [x] `a"` `i"` `a'` `i'` ``a` `` ``i` `` — quoted strings
- [ ] `at` `it` — tag blocks

## Operators (`:help operator`)

- [x] `d` — delete; a charwise range spanning lines that starts within the
  indent and ends before only trailing blanks becomes linewise, for delete
  ALONE: `de` on `"a\nbb  \nccc"` leaves `"ccc"`, while `ye` there yanks the
  fragment `"a\nbb"` and `ce` keeps the line
- [x] `c` — change, one undo step (see below)
- [x] `y` — yank, into the one register described below
- [x] `x` `X` `D` — character / to-end-of-line shorthands
- [x] `>` `<` — shift, by one indent of four spaces per count; see below
- [ ] `gu` `gU` `g~` — case
- [ ] `!` `=` — filter/indent

**A shift moves whole lines by one indent per count and leaves the cursor at
its DESIRED column**, not the first non-blank: measured, `>>` on "  abc" from
the first non-blank lands on column 2, `$>>` on the new line end, `<<` on
"    abc" from column 4 lands on column 2 of "abc" -- vim's `coladvance(curswant)`
under `nostartofline`, nvim's default. `>>` leaves an empty line alone; `<<`
takes up to one indent of leading spaces and does not re-tab a tab. Measured at
`shiftwidth=4 expandtab`, which the oracle sets because INDENT is what Tab and
`o` already insert; the whole range is one undo step.

**`aw` takes `count` units, where a unit starting on WHITESPACE is that
whitespace run plus the following word**, and a unit starting on a word is the
word alone. Trailing whitespace is appended only when the object BEGAN on a
word, falling back to leading whitespace that does not reach the line start;
once an object began on whitespace, every later unit carries its own gap. A unit
that cannot reach a word on the same line FAILS the object, and the visual path
applies the identical rule, resolving a repeat from the position after the
selection's end.

Measured against nvim over the operator and visual paths alike, with zero
disagreements; the rows that pin it are in `tools/textobject_cases.tsv`. Three earlier attempts stated the object half correctly and broke
`vawawd`; the cause was not the rule but an ORDERING bug in the visual path --
the guard deciding "is this a repeat" read the anchor after the line that moves
it. Capturing the comparison before the mutation is what makes both paths agree.

**A command that cannot act AT ALL fails, and a failure drops the rest of the
fed sequence.** "Fed" is the exact word: vim's beep discards the typeahead a
mapping or `feedkeys()` put there and never keys the user typed, so on a real
tty `fq` then `x` deletes the character, while `"fqx"` fed as one string --
by `ed_feed`, or by harness A -- leaves it. Keys sent one at a time through
`ed_key` are never dropped, and `ed_feed_aborted` says whether a fed string
was. A command that can partly serve a count clamps and succeeds, and one that
legitimately does nothing does not fail:

| | |
|---|---|
| aborts | `2dd` on one line, `u` at the root, `<C-r>` with nothing to redo, `J` on the last line, `~` on an empty line, a failed `f`/`t`/search, `di(` with no bracket, `99k` at the top |
| runs on | `3dd` on two lines (clamps), `9G`, `p` with an empty register, `x` or `D` on an empty line, `99j` at the bottom, `dh` or `d<BS>` at column 0 (the motion SUCCEEDS and the range comes out empty: nothing deleted, the next key runs) |

A yank of NOTHING still writes the register, empty: `xyhP` on `"abc"` gives
`"bc"`, the `a` that `x` took gone by the time `P` looks. A delete of nothing
leaves it alone — `xdhP` gives `"abc"`.

**An empty-range DELETE or CHANGE still opens an undo block**, so the `u` after
it succeeds and changes nothing rather than failing at the root. This is why
`d0` at column 0 and a failed `dTa` part company: both leave the buffer alone,
but only the failed motion aborts. Measured: `d0ux` on `"aaaa"` gives `"aaa"`
where a bare `ux` gives `"aaaa"`; exactly one `u` is armed, since `d0uux` gives
`"aaaa"` again; `y0ux` and `yyux` show a YANK of nothing arms nothing; and
`dTaux`/`dFaux` show the failed motion still aborting. Once any real edit is
recorded the block stops mattering, which is what `d0xux` and `xux` agreeing
says.

**A LINEWISE change of an empty line fills the register** with the line it took
— a newline, linewise — the same as `dd` and `yy` there: nvim's `jcc<Esc>P` on
"one / (empty) / three" leaves a second empty line. A CHARWISE empty change
does not touch the register at all: `yyci{X<Esc>P` on `"{}"` pastes the yanked
line.

Measured over 40 cases. The distinction is "cannot act" rather than "the count
overruns", which is why `2dd` and `3dd` differ on a two-line buffer.

**A third class MOVES AND FAILS**, so where the caret goes and whether the
sequence continues are two answers rather than one. `9gj` on "aa/bb/cc" lands on
line 3 and a following `x` does NOT run, while `9j` lands there and the `x`
does; `jj2gk` -- a count `gk` can serve in full -- moves and continues. Measured
over every motion below at an over-range and an under-range count, plain and
under `d`:

| | |
|---|---|
| clamps, moves, continues | `j` `k` `+` `-` `G` `gg` `h` `l` `w` `b` `ge` `g0` `g_` `$` `\|` `_` |
| clamps, moves, FAILS | `gj` `gk` `g$`, and `e`/`E` in the plain path only |
| fails without moving | `}` `{` `f`/`t` |

`e` is the split one: `4e` on "ab cd ef" reaches the third word end and drops
what follows, while `d4e` deletes the clamped range and the next key runs. So
the operator is exempt from the count check for `e` and is NOT for `gj`/`gk`,
where `d9gj` deletes nothing.

This is why `Motion_Result` carries `move_on_fail` beside `ok`: a single flag
cannot say "go there and stop". Three attempts to encode it inside the gj/gk
branch each fixed one half and broke the other.

`[count]g$` is the end of the count-th SCREEN line down, the way `[count]$` is
the count-th buffer line down -- `2g$` on "aa/bb/cc" reports line 2. Ignoring
the count put every one of them on line 1, which no cursor-only shape shows,
because the count is what moves the line.

**A paste puts the caret on the LAST pasted character, except a multi-line
charwise register**, which puts it on the insertion point -- "the last
character" is on a different line by then. Measured over 180 cases spanning
register source, direction and starting column, 180/180. A charwise register
gets a newline from `v$d`, or from `$ye` at a line's end; every single-line
shape keeps the last-character rule, which is what makes `xp` a transpose.

**`w` on the buffer's last character must FAIL, and does not.** A failed motion
drops the rest of a fed sequence, and this one still reports success. Measured
on "aa / dd": `G$w` and `Gw` both end on (1,1), but a following `k` runs after
`Gw` and does not after `G$w` -- same position, same desired column, opposite
outcome. Reporting failure when the position did not change is not sufficient,
because `$` leaves the caret one short of where `w` stops, so `w` still moves;
three attempts were reverted. The condition vim uses needs its own table. A
table over linewise-delete x following-motion found no other divergence, so
this is narrow rather than a family.

**A buffer's FINAL empty line is an empty line here as in vim** -- a text
ending in "\n" holds it -- and `iw` on it is the object described under the
text objects: `jdiw` on "a\n" and `3Gdiw` on "a\nb\n" are pinned rows. An
earlier round recorded this as a buffer-model gap; it was an object gap.

**`aw` on a blank line is ONE CHARWISE RANGE in every position** -- first line,
middle, last. From the blank line's newline: take that newline, then the
whitespace run after it (which crosses a line made only of blanks and keeps
going), then the word run; an EMPTY line ends the whitespace run instead, so the
object is then two line breaks and no text. `getregtype` reports charwise on
every shape and there is no linewise promotion anywhere.

The first-line case looks different only because of what DELETE does with that
range afterwards: it also takes the rest of the range's last line and the break
ending it, or the PRECEDING break when the range reaches the buffer's end. So
`daw` on "(empty) / bbbb / cc" leaves "cc" while `jdaw` on "a / (empty) / b c"
leaves "a / ␣c" -- same object, different operator behaviour. `caw` equals
`vawc` and `yaw` equals `vawy` in every measured case.

This corrects an earlier note here which claimed the two "cannot be the same
range". They are the same range; the inference that a linewise object was wrong
was sound, and the conclusion drawn from it was not.

**A blank line is a word object of ZERO width for `iw` and a whole-line one for
`aw`.** The cursor there sits on the newline, which classifies as whitespace, so
a run that did not stop at it swallowed the line break and the next line's
indent and joined the two. Measured: `diw` on the blank line of "a / (empty) /
4 spaces + b" removes nothing, `daw` there leaves "a" alone, and `diw` on
"one \n two" takes "one" while the trailing space, the newline and the next
line's leading space all stand. A run therefore stops at a newline; `d2iw` still
crosses one, because the COUNT steps over it rather than a run running through.

**A line opened by `o`, `O` or Enter is indented in SPACES**, expanded from the
source line's indent at the same rendered width -- `o` on a tab-indented line
writes four spaces per tab stop, which is nvim's answer under `'expandtab'` and
what types.odin requires of anything the editor writes. An indent that is
RESTORED rather than opened keeps its tabs: `cc` and `S` put back the line's own
leading whitespace verbatim, so a tab-indented line stays tab-indented.

**A yank moves the caret to the range's FIRST LINE at its desired column**,
clamped -- except a DOWNWARD SELECTION, which goes to column 0. A charwise range
moves only when its start is behind. Derived from a table over every combination of
start line, start column and selection direction, after four attempts to state
it from a handful of cases each traded one subset for another:

| | |
|---|---|
| `yy` `yj` `2yy` | stay; the first line is the caret's own |
| `yk` `ygg` | move up, keeping the column (`$` keeps end-of-line) |
| `Vy` `Vjy` `VGy` | column 0 |
| `Vky` `Vggy` | move up keeping the column, exactly as `yk` does |

The last row is why the discriminator is the selection's DIRECTION rather than
visual mode, and why it cannot be read off the range: `Vy` and `yy` build the
identical one. An earlier rule of "linewise stays, charwise moves" fit the first
two rows and got every downward selection wrong.

**A shift REPLACES the indent at a computed width**, rather than adding or
stripping bytes. Both directions and both whitespace kinds fall out of that one
rule, and a tab is re-indented: measured, `>>` on a tab-indented line gives
eight spaces at tab width 4, and `<<` on it gives none. Prepending spaces in
front of the tab left the line indented in two units at once, against
types.odin's rule that nothing this editor writes creates a tab.

**Case is UNICODE.** `~` uppercases `ü` to `Ü` and `ß` to `ẞ`, which
`core:unicode` decides rather than a hand-written ASCII range.

**`b` and `B` stop ON an empty line**, which vim counts as a word of its own --
`jwB` on "one / (empty) / three" lands on the blank rather than carrying to the
previous word. The same rule the `iw`/`aw` objects follow.

**`+` and `-` set the desired column** to the first non-blank they land on, so a
linewise operator after them keeps it: nvim's curswant after `2+` on an
eight-space indent is 8, and `3|2+dd` lands there rather than at column 0. Like
`|`, a Linewise motion otherwise sets no column at all.

**`|` counts SCREEN columns**, so a tab is one stop of the editor's tab width
rather than one character, and it keeps the column it ASKED for as the desired
one -- the way `$` keeps
end-of-line -- even when the line is too short to reach it: measured, `8|j` from
a two-character line onto a ten-character one lands on column 7, and `8|dd`
puts the caret on column 7 of the line that shifts up. Reading the desired
column back off the clamped caret gives 1 and loses the request -- which is
invisible until something later consumes curswant, so it surfaced only once
the linewise operators started to.

**`[count]X` deletes backwards, stopping at the line's start.** It is a no-op
at column 0 rather than joining the line above, and a count larger than the
room available takes only what is there: `l3X` on "alpha" gives "lpha". The
caret moves to the range's start before the delete, since that is where `u`
restores to -- `lXu` puts it on column 0. `x` never showed that difference,
because its range starts at the caret and the two coincide. In visual mode `X`
is linewise `d` whatever the selection was.

**A LINEWISE operator leaves the cursor at its DESIRED column too**, by the
same `coladvance(curswant)` rule the shifts use -- it is not the line start and
not the first non-blank. Measured on "aaaa / bbbbbbbbbb / 8 spaces + cccc":
`j8|dd` lands on column 7 and `j2|dd` on column 1, both INSIDE the indent that
a first-non-blank rule would skip, and `j$dd` lands on the new line's end
whatever its length. `dd`, `2dd`, `dj` and a visual `Vd` all obey it; `yy`
differs and leaves the cursor where it stood, since it removes nothing.

This shipped at column 0 for as long as the corpus asserted only text, and an
embedder found it by pressing `dd` -- which is why the e2e corpus now asserts
the cursor and `tools/fuzz_diff.py` compares it after every step.

**The cursor after `u` is the change's ORIGIN** — where vim's caret stood when
the change began, which an operator has already moved to its range's start.
For a charwise range that is the start (`$db` then `u` restores column 11,
`viwd` then `u` the word's start); for a linewise motion the caret after it
(`j$dk` then `u` the end of line 0, `jwd-` then `u` its first non-blank, `dd`
where the caret was); for a downward `V` selection column 0 of its first
line and for an upward one the caret itself (`jwVjd` then `u` column 0,
`jwVkd` then `u` column 4). A linewise CHANGE over more than one line records
the caret one line DOWN keeping its column — vim deletes the extra lines with
the caret on the second before saving — so `jw2ccX<Esc>` then `u` restores to
line 2 where `jwccX<Esc>` restores to line 1, and `jwV$kcZ<Esc>` to the END of
line 1, the column the selection's caret held on the newline. `Op_Range.origin`
carries it, and a yank leaves the caret at the same position. Sixty rows,
every operator, both visual modes. A host drawing a current-line strip from
`ed_cursor` has what it needs; a stale strip is the host's own frame.

**Redo lands where undo does**: at the origin, clamped by line and BYTE column
to the buffer as the redo leaves it. `wiZ<Esc>u<C-r>` restores column 4,
`jwd$u<C-r>` the last character of the line it emptied, `jjwd2ku<C-r>` — whose
origin's line is gone — the last line standing, `ON<Esc>u<C-r>` on a
tab-indented line column 1 of the `"    N"` it opened. 45 rows over every
node kind. The standard keymap keeps the caret after the redone text, as
non-modal editors do.

**The arrows do not wrap at a line's ends**, in insert mode or normal; `<BS>`
and `<Space>` do. nvim's `'whichwrap'` defaults to `"b,s"`, which lists those
two and neither arrow, so `i` then three `<Right>` on "ab" stays on line 1 at
column 2 rather than reaching line 2, while normal-mode `$<Space>` reaches it.
The insert-mode arrows once stepped by byte and walked over the newline; the
two wrapping keys are measured under the left-right motions above.

**`J` pads nothing onto nothing.** The join takes the next line's leading
whitespace and inserts one space, except when the first line already ends in
whitespace, when the next character is `)`, when there is no line below -- and
when the line joined onto is EMPTY, or only whitespace the trim consumed.
Measured: `J` on "one / (empty) / three" gives "one" with the caret at 2, where
a space would give "one " and caret 3.

**A change is ONE undo step**, as vim's is: `cwX<Esc>` then a single `u`
restores "word here". The delete node absorbs the typing that follows and
becomes a `Replace`, the shape a visual paste already used.

This was shipped broken first, and the way it broke is worth keeping. The
obvious fusion passed every unit and corpus case while `make fuzz` reported five
undo-to-root violations. The diagnosis written down at the time -- that a
visual-mode `c` entering insert immediately was the breaking shape -- was WRONG,
and re-checking it against a node dump is what found the real cause: a Replace's
`range` names the bytes standing in the buffer right now, and the fusion
OVERWROTE that end instead of growing it. Typing `iw`, jumping the cursor to
column 0 and typing `Z` shrank the range from `0..2` to `0..1` and orphaned the
`iw`, so undo left text behind.

**The trigger is a HOST API call, not a keystroke**, which is why no
keyboard-only test on either side would have found it. A cursor jump mid insert
session is what `ed_set_cursor` does, and a host doing completion or
jump-to-error makes that call routinely -- so this was reachable in ordinary use
rather than only under a fuzzer. Confirmed against the pre-fix build through the
ABI alone: `Vc`, type `iw`, `ed_set_cursor(0,0)`, type `Z`, undo -- `"Wiw"`
instead of `"W"`.

So the end grows by what each insertion adds, and an insertion that is not
contiguous with the node ends the change rather than extending across the gap.
Both halves are load-bearing and only one is visible to the unit tests:
dropping the contiguity check leaves every test green and returns four fuzzer
violations.

## Known divergences

None open in the keymap: `tools/fuzz_diff.py`'s KNOWN set is empty, and a
divergence its walks find fails `make fuzz`. One thing is recorded here
instead of being fixed, with its reason:

- **Harness A cannot see past a `.` that repeats a put.** Under
  `nvim_feedkeys` the repeat drops the rest of the typeahead: `yyp.x` reports
  the third line untouched where a real tty deletes its first character, and
  this editor agrees with the tty. `tools/fuzz_diff.py` suppresses that shape
  and names it; harness B decides such a row when one matters.

## Simple changes (`:help simple-changes`)

Not motions or operators, and previously absent from this file altogether —
which is the failure a checklist exists to prevent: a reader could not tell
these were unimplemented, because they were not listed as anything. Every one
of them is now implemented and oracle-checked.

- [x] `r` — replace one character
- [x] `R` — replace mode
- [x] `~` — toggle case of the character under the cursor
- [x] `J` — join lines, and `gJ` which keeps the whitespace
- [x] `S` — substitute whole line (`cc`)
- [x] `>>` `<<` — shift the current line, with a count of lines
- [x] `%` — jump to the matching bracket
- [x] `.` — repeat the last change

`r`, `~`, `J`/`gJ` and `S` live in `src/simple_change.odin` (`R` in the insert
path, since it is a mode). What each does was measured rather than recalled,
and four answers contradict the obvious guess:

- **`J` trims one side and pads the other.** The next line's leading
  whitespace goes; the first line's TRAILING whitespace stays, and the single
  space is inserted only when there was none. `"foo  "` joined to `"   bar"`
  gives `"foo  bar"` with both spaces kept and the cursor at column 5. No
  space is inserted before a `)`.
- **`r` fails outright when the count exceeds the line**, where `~` takes what
  it can: `5rZ` on `"abc"` changes nothing, `9~` on `"abc"` toggles all three.
- **`R` is one undo step, takes a count, and its Backspace RESTORES.**
  `Rxyz<Esc>` then one `u` gives back the original line; `2Rxy<Esc>` on
  `"abcdef"` gives `"xyxyef"`, since a count repeats the typed text where it
  left off; and `Rx<BS><Esc>` leaves the line exactly as it found it, because
  backspace in replace mode puts the overwritten character back rather than
  deleting one. Past the end of the line there is nothing to restore, so
  `Rxyz<BS><Esc>` on `"ab"` does shorten what it grew.
- **`r<CR>` splits the line** rather than writing [count] newlines.
- **`S` and `cc` auto-indent, and vim takes the indent back** if the session
  ends with nothing typed: `ccZ<Esc>` on `"  aaa"` gives `"  Z"` while
  `cc<Esc>` empties the line. That rule belongs to every command that opens a
  line, `o` included, and to a `<CR>` typed inside the session: the line it
  leaves gives up an untouched indent too — on `<Esc>`, on another `<CR>`, on
  `<Up>`/`<Down>` — while the indent still seeds the next line, so
  `A<CR><CR>y<Esc>` on `"    abc"` leaves an empty middle line and `"    y"`
  under it. Typing into the line makes the indent the user's even when the
  typing is deleted again: `oz<BS><Esc>` on `"  abc"` keeps the two spaces.

**`.` records the KEY SEQUENCE of the last change and replays it** through the
same dispatcher that ran it the first time (`src/repeat.odin`). Replaying keys
rather than a resolved range is what makes the hard case work: `ciwZ<Esc>w.`
must resolve `iw` at the NEW cursor, which a stored range cannot do.

The record is not a keystroke log. Keys accumulate per phrase and are COMMITTED
where a change completes, so a motion, a yank or an undo leaves the previous
record standing — which is why `dwyw.` repeats the `dw`.

Measured rules, each pinned by a case:

- a count on `.` **replaces** the original's rather than multiplying it:
  `dw3.` on `a b c d e f` leaves `e f`, three words and not six. The count is
  stored APART from the keys, both halves multiplied, so it replaces the
  product: `2d3w` then `4.` deletes four words, `3x2.` five characters in all,
  `2iA<Esc>3.` three more `A`s. With the digits left inside the keys `3x2.`
  replayed `23x`;
- a put is a change: `yyp.` pastes the line twice, `dwp.` repeats the put and
  not the delete;
- `.` with nothing to repeat FAILS and drops the fed sequence: `.x` on a
  fresh buffer leaves it alone;
- a change made on a SELECTION replays as vim's `redo_VIsual`: a selection of
  the same SIZE is re-made at the caret -- the same number of lines, and on
  the last of them the same width (one line) or the same display column
  (several), or the line's end after `$` -- and then the operator's own keys
  run, count included (`V2>.` shifts twice). Replaying the motions that built
  the selection re-resolved them where the caret now stood: `vwd.` deleted to
  the next word's start, `viwd.` a different word. A count on `.` is ignored
  there, as vim ignores it: `vld3.` deletes two more characters;
- an insert session replays its TYPED TEXT and its cursor placement, so
  `ihello<Esc>.` on `x` gives `hellhelloox`;
- each repeat is its own undo step: `dw.u` on `a b c d` leaves `b c d`;
- a repeat never re-enters a repeat. The record is keys, so a phrase holding
  `.` would replay `.` for as long as the stack lasts — reachable through
  documented ABI calls alone, and found by the fuzzer the moment its generator
  learned to type `.` at all.

**Two of those rules are invisible to harness A.** `nvim_feedkeys` injects
typeahead, so vim never closes the change block: the batch oracle reports
`X y z` for `~..` where a terminal gives `X Y z`, and one undo state for `dw.`
where a terminal makes two. Those rows are measured with harness B and are
deliberately absent from the corpus — a row that measures the harness instead
of vim is worse than no row.

Operator + motion composes through the motion's class: an exclusive motion stops
before the end position, an inclusive one includes it, and a linewise motion
takes whole lines. Doubling the operator (`dd`, `cc`, `yy`) is linewise.

That composition is not the whole story, and the exceptions are where an
operator implementation written from intuition goes wrong. All of these are
quoted from vim's own docs and pinned by cases:

- **The two general exceptions** (`:help exclusive`, `*exclusive-linewise*`).
  If an exclusive motion ends in **column 1**, the end moves back to the end of
  the previous line and the motion becomes inclusive; if additionally the start
  was at or before the line's first non-blank, it becomes **linewise**. Without
  these, `dw` on a line's last word leaves a stray newline.
- **The `w`-with-operator rule** (`:help word`, "Another special case"). When
  `w` is used with an operator and the **last word moved over** ends a line,
  the end of that word bounds the operated text — not the first word on the next
  line. The bound is the end of that *word*, not of its line. From an EMPTY
  line the last step stops at the START of the next line instead — vim's
  `fwd_word` stops the moment it crosses a break under an operator — and the
  two exceptions above then make `dw` there LINEWISE and `cw` a no-op that
  keeps the line to type into: `dw` on the empty line of
  `"alpha\n\n    delta"` leaves the indent standing, `cwQ<Esc>` there gives
  `"alpha\nQ\n    delta"`. Reaching for the next line's first non-blank joined
  it up, which `Ddw` showed on every emptied line.
- **A range PROMOTED to linewise lands on the first non-blank** of the line
  that moves up, where `dd` keeps its column: `$jdw` on the same buffer lands
  on column 4 and `j$dd` on `"aa\n    bbbb\ncccccccccccc"` on column 11. A change
  promoted the same way is linewise too, register included — `c}Q<Esc>p` on
  `"aa\nbb\n\ncc"` pastes `aa` and `bb` below the `Q`.
- **`cw` is treated like `ce` on a non-blank** (`:help word`, "Special case"),
  but only while the word extends past the cursor: on a word's last character
  `ce` runs to the next word's end while `cw` takes just that character.
- **Change and delete differ at line boundaries.** `cc` keeps an empty line to
  insert into where `dd` removes the line, and a change never extends its start
  back over leading indent — `'startofline'` applies to `d`, not `c`.
- **A motion that cannot move fails**, and a failed motion aborts the pending
  operator rather than applying it to a degenerate range. A *partial* move still
  counts: `3b` near the buffer start lands on column 0 and succeeds.
- **Counts multiply**: `2d3w` is `d6w`. Verified.

## The register (`:help registers`)

**ONE register, not twenty-six.** Every delete, change and yank writes it, and
`p`/`P` paste it; last write wins, so a `yy` after a `dd` replaces what `dd`
took. Named registers (`"ayy`), the numbered ring (`"1p`), the small-delete
register and the read-only ones are all out of scope — a second slot buys a
syntax and a lifetime question for a case that does not come up in practice.

- [x] `p` `P` — paste after / before, with a count (`3p` pastes three times)
- [x] `p` `P` over a visual selection — the selection is replaced, as one
  undo step
- [x] `y` `d` `c` `x` and friends all write the one register
- [ ] `"a`–`"z` named registers, `"0`–`"9` the numbered ring
- [ ] `:registers`

Measured against nvim, and several are not what intuition says:

- **`ddP` and `dwP` and `xP` restore the original text**, because paste-before
  puts back exactly what the delete took.
- **`xp` transposes** — the classic — because charwise paste lands AFTER the
  cursor's character and leaves the cursor on the last pasted one.
- **A linewise paste puts the cursor on the first non-blank of the first pasted
  line**, not at the column it was in.
- **A linewise register always ends with a newline**, even when it came from
  the buffer's last line, which has none. `dd` on the final line stores
  `"gamma\n"` with regtype `V`; storing the raw slice instead makes the next
  paste join two lines.
- **A change writes the register too**, so `cw` then `p` pastes what `cw`
  removed.
- **A visual `p` writes the register with what it replaced, and `P` does
  not**, so `vep` twice swaps two words while `veP` twice pastes the same
  word twice. The SELECTION's shape decides the form of the put, not the
  register's: a linewise selection takes a charwise register as one line of
  its own (`yiwjVp` on "alpha/beta/gamma" gives "alpha/alpha/gamma"), and a
  charwise selection splits its line around a linewise register (`yyjvep` on
  "alpha/beta gamma/delta" gives "alpha//alpha/ gamma/delta"). The cursor
  follows the put's form: last character of a charwise one, first non-blank
  of a linewise one.
- **An emptied buffer still has one line.** `dd` on a one-line buffer leaves an
  empty buffer, and `p` pastes BELOW that empty line -- `["", "a"]` -- while `P`
  pastes above it, `["a", ""]`. Treating the empty case as a plain insert gives
  the `P` answer for both: the same characters in the wrong order, which reads
  as correct until the blank line is noticed.

A host wiring the SYSTEM clipboard uses `ed_selection_text` with
`ed_replace_selection` / `ed_insert_at_cursor`, or writes this register through
`ed_set_register` so its own paste and `p` agree; see `ffi/README.md`.

## Scrolling (`:help scrolling`)

Cursor-moving and view-only, and the split matters: the first four are MOTIONS
and live with the cursor logic, so a host that scrolled its own view instead
left the cursor behind and a later edit acted off-screen.

- [x] `<C-d>` `<C-u>` — half a window down / up, cursor and view together
- [x] `<C-f>` `<C-b>` — a full window, less two lines of context
- [x] `<C-e>` `<C-y>` — the view alone, a line at a time
- [x] `zz` `zt` `zb` — the cursor's line to the middle / top / bottom
- [ ] `<C-d>`'s count PERSISTING as vim's `scroll` option
- [ ] `zh` `zl` and horizontal scrolling
- [ ] `scrolloff`

Measured at a 20-row window on a 40-line buffer, cursor on line 1:

    <C-d>  -> 11    half the window, plus the line it started on
    <C-f>  -> 19    a window less two, so the last two lines head the new view
    <C-u>  -> 1     clamped
    2<C-d> -> 3     a count is the DISTANCE, not a repeat

**The count does not persist.** In vim, `2<C-d>` sets the `scroll` option and a
later bare `<C-d>` moves 2. That is a stateful option this editor does not
otherwise have, and the oracle resets it between cases so no expectation
depends on it.

**`zt` and `zb` clamp where vim does not.** vim lets the last line sit at the
top of the window with blank space below — `30Gzt` on a 40-line buffer reports
topline 30. This editor clamps to `scroll_max`, the largest offset that still
shows content, which is the rule the wheel and every jump-to-line already
follow and which the ABI exposes as `ed_scroll_max`. Following vim for `zt`
alone would mean changing all of them, so the divergence is deliberate.

The view-only commands report an offset rather than applying it, because the
scroll offset lives in the HOST — two views of one buffer scroll independently.
See `ed_take_scroll_request` in `ffi/README.md`.

## Searching (`:help search-commands`)

- [x] `/` `?` — search forward / backward, sharing the `:` command line
- [x] `n` `N` — repeat, in the search's own direction or reversed
- [x] `*` `#` — search for the word under the cursor, whole; `g*` `g#` the
  substring forms
- [x] `hlsearch` `incsearch` — every match lit, and the typed prefix while
  the `/` line is open, as a `Search_Match` primitive; a normal-mode Escape
  is `:nohlsearch`, and `n` lights it again

**`*` takes the word nearest the cursor in vim's order**: the keyword under
it, else the first keyword after it on the line, else the non-blank run under
or after it. Only a keyword is searched whole, so `*` on "foo" skips "foobar"
while `*` on "..." finds any "..."; `g*` never searches whole. The cursor is
moved to the word's START before the search, which is how `#` mid-word does
not find the word it is on. `n` keeps `*`'s whole-word rule until the next
`/`.

**The pattern is a LITERAL string, not a regex.** vim's regex is neither POSIX
nor PCRE, so matching vim here means implementing vim's dialect; anything less
is a third dialect that merely looks like it. Literal search is what a host's
find box does, and `ffi/README.md` says so at the boundary where a host would
otherwise assume.

Measured, and not what intuition says:

- A search that finds nothing ABORTS the rest of the fed sequence, the way a
  failed `f` does — `/zzz<CR>x` leaves the text untouched.
- `N` reverses the direction the search was ENTERED with, so after `?foo` it
  goes forward. The direction belongs to the search, not to the key.

## Ex commands (`:help ex-cmd-index`)

A deliberately small slice, not the ex language: `:w`, `:q`, `:wq`/`:x`, and
substitution. Everything else is out of scope below.

- [x] `:w` `:q` `:wq` `:x` — recognised, with `!` and an optional path, and
  reported to the host: the editor owns no file and no window
- [x] `:s/old/new/` `:%s/old/new/g` — substitute, literal pattern
- [ ] a range other than `%` — `:1,5s`, `:.,+3s`
- [ ] the `c` (confirm) and `i`/`I` flags

The separator is whatever follows `s`, so `:s#a/b#c#` works — which is what a
pattern containing a slash needs. Measured, and again not what intuition says:

- Without `g`, the FIRST match on each line is replaced, not the last.
- The cursor lands on the first non-blank of the LAST substituted line, not
  where it started.

## Indenting while typing (`:help 'smartindent'`)

Not keymap and not oracle-decided: `nvim --clean` has `autoindent` on and
`smartindent` and `cindent` off (measured: `ai=true si=false cin=false`), so the
corpus carries plain autoindent and nothing of the rest; these are pinned by
hand in `src/behavior_test.odin`, each switchable through `Behavior`.

- [x] a new line carries the indent of the line it came from (`autoindent`)
- [x] a line ending in `:` or `{` indents the next one a step further
- [x] a closing bracket typed as the first non-blank of its line takes the
  indent of the line holding its matching opener

**The closer snap is `smartindent`'s `}` rule, generalized by decision.**
Measured in nvim with `smartindent` on: `A<CR>x;<CR>}<Esc>` on `void f() {`
puts the brace at column 0, the nested `if` inside it puts the inner brace at 4
and the outer at 0, and one `u` restores the original line — the snap is part
of the insert session's single undo step. Three places this editor departs
from `smartindent`, each measured. It snaps `)` and `]` as well as `}`, since
the pair table knows them and the alternative is a paren list whose closer
drifts a step right (smartindent leaves `)` where it was typed). It snaps a
closer whether or not the line's indent was typed in the same session
(smartindent moves a `}` only when autoindent placed that indent, so `A}` on a
line indented before the session stays at its indent; `cindent` moves it, as
this does). And it takes the OPENER'S line indent exactly — `x = (` with an
over-indented body puts `)` at 0 here, where smartindent leaves it at the
body's 8 and cindent puts it at 4 — because the maintainer asked for the
opener's indent, and because the current indent is already wrong once one
closer above was left unsnapped: the new-line indent copies it down and the
error compounds through the block. An unmatched closer stays where it was
typed (measured: `A}` on a blank indented line leaves `    }`), and a closer
typed after text on its line moves nothing.

## Various (`:help various`)

- [ ] `K` — keyword lookup. **Deliberately unbound in normal and visual
  mode** (insert mode types the letter, as vim does). vim's `K` runs
  `keywordprg`, an external program; here the external program is the host,
  which owns the documentation for the words it knows. The key reaches it as a
  false from `ed_key` with nothing changed, the seam every unbound key uses, and
  `src/chord_test.odin` pins that a binding cannot silently take it back.

## Out of scope

Marks, macros, named and numbered registers, and the ex language beyond the
handful above — named in the design's vim scope. The single unnamed register is
implemented; see above.
